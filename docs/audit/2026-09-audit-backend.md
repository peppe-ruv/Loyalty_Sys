# Audit di Conformità Architetturale del Backend (Settembre 2026)

| ID | Gravità | Servizio | File:Linea | Regola violata (Doc) | Evidenza | Soluzione proposta | Test di regressione | Confidenza | Stato |
|---|---|---|---|---|---|---|---|---|---|
| AUD-BE-01 | Alta | member-service | `SegmentRefresher.java:194` | `docs/06 §1` (outbox dentro `@Transactional`) | `outbox.write(events.newRoot(...))` chiamato all'interno di `reannounceAll` via transaction template, ma le call dirette ad altri metodi come `announceLeftSince` e `reannounceLater` non sono annotate con `@Transactional`. | Aggiungere `@Transactional` a `announceLeftSince` o invocare la outbox sempre in un template di transazione. | Test di rollback su failure durante il reannounce dei segmenti in demo mode. | Probabile | non corretto: `announceLeftSince` è chiamato solo da metodi già transazionali (reset demo, ricalcolo); nessuna scrittura fuori transazione verificata |
| AUD-BE-02 | Media | campaign-service | `ConditionEvaluator.java:269` | `docs/06 §9` (Determinismo test con `Clock` fisso) | `member.birthDate().until(LocalDate.now(ROME)).getYears()` ignora il `Clock` del sistema che è essenziale per il simulatore e i test. | Passare il `clock` al metodo (o costruttore dell'evaluator) ed eseguire `LocalDate.now(clock.withZone(ROME))`. | Unit test che valuta la condizione `age` simulando date nel passato/futuro tramite un `Clock` fisso. | Certa | corretto: età al giorno di business dell'azione (Europe/Rome); test `CampaignEngineTest.memberAgeUsesTheActionDayInRome` |
| AUD-BE-03 | Media | insight-service | `EventIngestService.java:149` | `docs/06 §9` (Determinismo temporale) | `Instant.now().atZone(ZoneOffset.UTC)` usato invece dell'orologio iniettabile. | Sostituire `Instant.now()` con `clock.instant()` dopo aver iniettato il `Clock` in `EventIngestService`. | Eseguire aggregazioni test con `Clock` fisso verificando l'orario delle metriche prodotte. | Certa | corretto: `Clock` iniettato |
| AUD-BE-04 | Media | insight-service | `EventIngestService.java:96` | `docs/06 §9` (Determinismo temporale) | `record.timestamp() > 0 ? ... : Instant.now()` | Utilizzare `clock.instant()` nel fallback invece di `Instant.now()`. | Test su ingest fallito che usa il clock fisso per la DLQ entry creation. | Certa | corretto: `Clock` iniettato |
| AUD-BE-05 | Media | insight-service | `TraceService.java:81` | `docs/06 §9` (Determinismo temporale) | `Duration.between(last, Instant.now()).toMillis()` | Utilizzare `clock.instant()`. | Unit test trace con clock fisso simulato a differenti orari. | Certa | corretto: `Clock` iniettato |
| AUD-BE-06 | Media | insight-service | `DlqRepository.java:46` | `docs/06 §9` (Determinismo temporale) | `Timestamp.from(e.firstSeenAt() == null ? Instant.now() : ...)` | Utilizzare clock al posto di `Instant.now()`. | Unit testing del repository. | Certa | corretto: `Clock` iniettato |
| AUD-BE-07 | Alta | insight-service | `EventIngestService.java:98` | `docs/06 §9` (Id non deterministici in demo/test) | `Ulid.next()` invocato senza passare il `Clock`. Questo rende random gli ID nei run demo limitandone il determinismo. | Passare `clock` e chiamare `Ulid.next(clock)` in `EventIngestService`. | Test che il seeder/consumer generi ID predicibili con un fixed clock per la voce DLQ. | Certa | corretto: `Ulid.next(clock)` |
| AUD-BE-08 | Alta | insight-service | `EventIngestService.java:199` | `docs/06 §9` (Id non deterministici in demo/test) | `Ulid.next()` al posto di `Ulid.next(clock)` per il logging di audit. | Modificare in `Ulid.next(clock)`. | Test che verifica predicibilità Audit record per demo. | Certa | corretto: `Ulid.next(clock)` |
| AUD-BE-09 | Bassa | reward-service | `RedemptionService.java:383` | `docs/06 §2` (Paginazione max size 100) | `int s = Math.max(1, Math.min(size, 200));` non rispetta il limite massimo di sistema documentato a 100. | Sostituire il limite superiore 200 con 100. | Chiamare l'API `/v1/redemptions?size=150` e asserire `page.size == 100`. | Certa | corretto: massimo 100; test `CouponIT.pageSizeIsCappedAt100` |
| AUD-BE-10 | Bassa | reward-service | `CouponService.java:192` | `docs/06 §2` (Paginazione max size 100) | `Math.min(size, MAX_PAGE_SIZE)` con `MAX_PAGE_SIZE = 200` alla linea 64 non rispetta il tetto globale. | Impostare `MAX_PAGE_SIZE = 100`. | Chiamare API GET coupons con size > 100. | Certa | corretto: massimo 100; test `CouponIT.pageSizeIsCappedAt100` |
| AUD-BE-11 | Bassa | wallet-service | `PortalActivityController.java:42` | `docs/06 §2` (Paginazione max size 100) | `ledger.listByMember(..., Math.min(Math.max(size, 1), 200))` supera il vincolo. | Sostituire 200 con 100 nell'invocazione. | API call al portal list ledger ed equals con 100 max count result. | Certa | corretto: massimo 100 |
