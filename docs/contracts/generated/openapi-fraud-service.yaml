# Contratto REST di fraud-service — generato da OpenApiContractTest, non modificare a mano.
# Rigenerare con: mvn -pl fraud-service test -Dopenapi.write=true
---
openapi: "3.1.0"
info:
  title: "Loyalty Hub — fraud-service"
  description: "API del servizio fraud-service. Scritture con chiave di idempotenza,\
    \ liste con paginazione keyset (after, size), errori con codice motivo UPPER_SNAKE."
  license:
    name: "Apache-2.0"
    url: "https://www.apache.org/licenses/LICENSE-2.0"
  version: "v1"
paths:
  /v1/risk/simulate:
    post:
      tags:
      - "risk-controller"
      operationId: "simulate"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SimulationRequest"
        required: true
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/Assessment"
  /v1/risk/members/{memberId}/assess:
    post:
      tags:
      - "risk-controller"
      operationId: "assess"
      parameters:
      - name: "memberId"
        in: "path"
        required: true
        schema:
          type: "string"
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/Assessment"
  /v1/risk/top:
    get:
      tags:
      - "risk-controller"
      operationId: "top"
      parameters:
      - name: "limit"
        in: "query"
        required: false
        schema:
          type: "integer"
          format: "int32"
          default: 50
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                type: "array"
                items:
                  type: "object"
                  additionalProperties: {}
  /v1/risk/policy:
    get:
      tags:
      - "risk-controller"
      operationId: "policy"
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/RiskPolicy"
  /v1/risk/members/{memberId}:
    get:
      tags:
      - "risk-controller"
      operationId: "current"
      parameters:
      - name: "memberId"
        in: "path"
        required: true
        schema:
          type: "string"
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                type: "object"
                additionalProperties: {}
  /v1/risk/members/{memberId}/history:
    get:
      tags:
      - "risk-controller"
      operationId: "history"
      parameters:
      - name: "memberId"
        in: "path"
        required: true
        schema:
          type: "string"
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                type: "array"
                items:
                  type: "object"
                  additionalProperties: {}
components:
  schemas:
    MemberActivity:
      type: "object"
      properties:
        memberId:
          type: "string"
        redemptions24h:
          type: "integer"
          format: "int32"
        accountsOnSameDevice30d:
          type: "integer"
          format: "int32"
        unitsEarned24h:
          type: "integer"
          format: "int64"
        avgDailyUnits30d:
          type: "number"
          format: "double"
        accountAgeHours:
          type: "integer"
          format: "int64"
        unitsEarnedFirst24h:
          type: "integer"
          format: "int64"
        maxSpeedKmh:
          type: "number"
          format: "double"
        failedCodeAttempts1h:
          type: "integer"
          format: "int32"
        transactions30d:
          type: "integer"
          format: "int32"
        returns30d:
          type: "integer"
          format: "int32"
        distinctDevices7d:
          type: "integer"
          format: "int32"
        events1h:
          type: "integer"
          format: "int32"
    RiskPolicy:
      type: "object"
      properties:
        id:
          type: "string"
        version:
          type: "string"
        signals:
          type: "object"
          additionalProperties:
            $ref: "#/components/schemas/SignalSpec"
        mediumFrom:
          type: "integer"
          format: "int32"
        highFrom:
          type: "integer"
          format: "int32"
        criticalFrom:
          type: "integer"
          format: "int32"
        autoBlockLevel:
          type: "string"
        decayHours:
          type: "integer"
          format: "int32"
        minTransactionsForRefundRatio:
          type: "integer"
          format: "int32"
    SignalSpec:
      type: "object"
      properties:
        enabled:
          type: "boolean"
        weight:
          type: "number"
          format: "double"
        threshold:
          type: "number"
          format: "double"
        saturation:
          type: "number"
          format: "double"
        description:
          type: "string"
    SimulationRequest:
      type: "object"
      properties:
        activity:
          $ref: "#/components/schemas/MemberActivity"
        policy:
          $ref: "#/components/schemas/RiskPolicy"
    Assessment:
      type: "object"
      properties:
        memberId:
          type: "string"
        score:
          type: "integer"
          format: "int32"
        level:
          type: "string"
        reasonCodes:
          type: "array"
          items:
            type: "string"
        reasons:
          type: "array"
          items:
            $ref: "#/components/schemas/Reason"
        assessedAt:
          type: "string"
          format: "date-time"
        policyVersion:
          type: "string"
    Reason:
      type: "object"
      properties:
        code:
          type: "string"
        observed:
          type: "number"
          format: "double"
        intensity:
          type: "number"
          format: "double"
        contribution:
          type: "number"
          format: "double"
        description:
          type: "string"
