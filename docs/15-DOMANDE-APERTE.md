# 15 — Domande aperte

Qui finiscono i dubbi che le specifiche non risolvono. Regola per l'agente (`CLAUDE.md §6`): **non fermarsi** su una domanda non bloccante — applicare il *default proposto*, marcare il codice con `// SPEC-GAP: Q-nn` e proseguire. Fermarsi solo sulle domande marcate **BLOCCANTE**.

Stati: `APERTA` · `DECISA` (riportare la decisione e, se architetturale, l'ADR) · `SUPERATA`.

## Domande del proprietario del progetto

| ID | Domanda | Default proposto (in uso finché non si decide) | Impatto | Blocca | Stato |
|---|---|---|---|---|---|
| Q-01 | Quale licenza? | **Apache-2.0** (ADR-022). Alternativa: AGPL-3.0 | file `LICENSE`, intestazioni, README | M0.1 (basta confermare) | APERTA |
| Q-02 | Nome del nuovo repository e dell'organizzazione GitHub? | `loyalty-hub`, repo pubblico sotto l'account personale | URL nel README, nel Demo Hub, nei nomi delle immagini | M0.1 | APERTA |
| Q-03 | Il brand demo **"Club Aurora"** va bene? (nome, colori, tono dei testi) | sì; è tutto in `seed/theme.json` e `seed/contents.json`, sostituibile senza toccare il codice | solo dati | — | APERTA |
| Q-04 | Autenticazione verso il Kafka gratuito: certificato client (`SSL_PEM`) o `SASL_SSL`? | `SSL_PEM` (predefinita dal fornitore); il codice supporta entrambe | variabili d'ambiente | M1.8 | APERTA |
| Q-05 | Il piano gratuito di Vercel è per uso **non commerciale**: la demo resta personale/open source? | sì. Se la demo servirà a fini aziendali → piano a pagamento o altro hosting statico | hosting del web | M1.8 | APERTA |
| Q-06 | Proteggere gli endpoint demo (reset, job) con una chiave condivisa? | no in M1 (dichiarato nel Demo Hub); `X-LH-Demo-Key` come P1 (`docs/11 §11`) | proxy + filtro in `lh-common` | — | APERTA |
| Q-07 | Lingua dell'interfaccia: solo italiano o anche inglese per il pubblico open source? | interfaccia solo in italiano, con dizionario unico pronto a essere tradotto; codice, API ed eventi in inglese; **README in italiano con una sezione breve in inglese** | `lib/i18n`, README | — | APERTA |
| Q-08 | Soglia oltre la quale una campagna richiede l'approvazione legale | budget > 100 000 punti o `requiresLegal=true` | policy in M7 | — | APERTA |
| Q-09 | Dominio applicativo dei dati demo: multiutility (bolletta digitale, autolettura) o generico retail? | multiutility **immaginaria**, senza alcun riferimento ad aziende reali; i tipi azione sono dati, quindi sostituibili | `seed/event-types.json`, `seed/campaigns.json` | — | APERTA |
| Q-10 | Serve un dominio personalizzato per la demo? | no: `*.vercel.app` e `*.onrender.com` | CORS, variabili | — | APERTA |

## Domande tecniche da verificare in corso d'opera

| ID | Domanda | Default proposto | Quando si verifica | Stato |
|---|---|---|---|---|
| Q-20 | Tempo reale di avvio di un servizio Spring Boot 4.1 su 0,1 CPU / 512 MB | atteso 60–150 s; oltre 180 s si attiva la cache AOT/CDS (`docs/11 §5`) | M0.5 (misurare con `docker run --cpus 0.1 -m 512m`) e M1.8 | APERTA |
| Q-21 | I minuti di build gratuiti di Render bastano per 8 immagini Docker? | sì con `buildFilter`; altrimenti piano B con immagini su GHCR (`docs/11 §7`) | M1.8, poi a ogni milestone | APERTA |
| Q-22 | Il client Kafka accetta i PEM in linea (`ssl.keystore.type=PEM`) con la chiave fornita, o serve la conversione PKCS#8? | convertire sempre in PKCS#8 senza passphrase | M1.8 | APERTA |
| Q-23 | Il pooler di Neon (modalità transazione) è compatibile con tutte le query previste (`FOR UPDATE SKIP LOCKED`, advisory lock `xact`)? | sì; Flyway usa la connessione diretta | M1.8 | APERTA |
| Q-24 | Con 2 partizioni per topic e `concurrency=2`, più istanze dello stesso servizio non servono: confermare che il piano gratuito non ne avvii più d'una | una sola istanza per servizio | M1.8 | APERTA |
| Q-25 | Un consumer che dorme più di 3 giorni perde eventi (retention Kafka). Serve un meccanismo di riallineamento? | no nel PoC: lo stato sta nei DB dei proprietari; per gli snapshot, `POST /v1/demo/reset` riallinea tutto. Nel target: retention lunga o topic compattati per gli snapshot | M2 | APERTA |
| Q-26 | L'SSE diretto verso insight regge il passaggio attraverso il proxy del fornitore (buffering, timeout a 100 s)? | `heartbeat` ogni 15 s + `X-Accel-Buffering: no`; in caso contrario polling | M2.2 | APERTA |
| Q-27 | Librerie da confermare su Boot 4.1: `springdoc-openapi`, validatore JSON Schema (`networknt`), ULID | scegliere l'ultima versione compatibile; se manca, alternativa minima scritta a mano (ULID) | M0.2 | APERTA |
| Q-28 | Generare i tipi TypeScript dall'OpenAPI dei servizi invece di scriverli a mano? | a mano fino a M4; poi valutare `openapi-typescript` in CI | M4 | APERTA |

## SPEC-GAP segnalati durante lo sviluppo
_(vuoto: l'agente aggiunge qui le voci `Q-40+` con file, riga, scelta fatta e motivo)_

| ID | Dove | Dubbio | Scelta conservativa adottata | Stato |
|---|---|---|---|---|
| Q-40 | `libs/lh-common` test (`docs/06 §9`) | La specifica indica Testcontainers (Kafka + Postgres), ma nell'ambiente di sviluppo il pull delle immagini Docker è negato dalla policy del proxy (docker.io/ECR/GHCR/Quay → 403). | Test d'integrazione con **Kafka in-JVM** (`EmbeddedKafka` KRaft, `spring-kafka-test`) e **Postgres reale in-process** (Zonky `embedded-postgres`), entrambi da Maven Central. Stesso comportamento verificato (outbox, idempotenza, DLQ, migrazioni Flyway). Se in CI il registry Docker è raggiungibile si può tornare a Testcontainers senza cambiare il codice di produzione. | APERTA |
| Q-41 | ambiente / `pom.xml` / CI | JDK locale predefinito 21, ma la spec richiede Java 25 (ADR-005). | **DECISA** (M0.7): CI su `temurin` **25** (`.github/workflows/ci.yml`, job `backend`); `pom.xml` con `release=25`. In sessione il JDK 25 è in `/opt/jdk-25`. | DECISA |
| Q-42 | `contracts/` | docs/05 §9 colloca gli esempi in `contracts/examples/`, ma `CLAUDE.md §3` indica `contracts/events/ … + examples/`. | Seguita la struttura di `CLAUDE.md §3` (`contracts/events/examples/`) — già scaffoldata in M0.1 — mantenendo il nome file `<famiglia>.<nome>.json` di §9. Da allineare i due documenti a una milestone futura. | APERTA |
| Q-43 | tutto il backend | Spring Boot 4.1 usa **Jackson 3** (`tools.jackson`) come default del web, mentre il validatore JSON Schema `networknt` usa Jackson 2 (`com.fasterxml`). La specifica non indica la versione. | Runtime (envelope, web, componenti) su **Jackson 3**; Jackson 2 confinato ai soli test di contratto (`ContractsTest`, con `networknt`). `LhJson` costruisce il mapper Jackson 3; i servizi lo ottengono da Spring Boot. Valutare un converter Jackson 3 per lo schema quando `networknt` lo supporterà. | APERTA |
| Q-44 | `campaign-service/CampaignEngine.java` | Il comportamento con numeri decimali/non interi in modo `FROM_FIELD` non è chiaro. | **Scelta conservativa**: I valori decimali in `FROM_FIELD` sono scartati, ritornando `null` (e scartando l'effetto). | APERTA |
| Q-45 | `wallet-service/WalletService.java` | L'elenco dei motivi di rettifica in `docs/03 §5` differisce da `wallet-service.md`. | **Scelta conservativa**: Usata la lista da `wallet-service.md` (`GOODWILL`, `CORRECTION`, `COMPLAINT`, `TEST`) per la regola di precedenza della fonte. | APERTA |
| Q-46 | `wallet-service/WalletService.java` | È consentita la rettifica di valute non spendibili (es. STS)? | **Scelta conservativa**: Non consentita. La modifica di STS cambierebbe `periodSts` e tier, impattando altre logiche. Restituisce `422 CURRENCY_NOT_ADJUSTABLE` finché non diversamente specificato. | APERTA |
| Q-47 | `wallet-service/ExpiryPolicy.java` | `END_OF_EDITION_PLUS_GRACE` (docs/03 §4.2: i lotti scadono a `edition.redemptionGraceUntil`) non dice cosa fare se nessuna edizione copre la data di guadagno, né il ruolo di `graceDays` indicato da BO-08. | Scadenza = fine giornata (Europe/Rome) di `redemptionGraceUntil` dell'edizione che contiene la data; se assente, `endDate + graceDays`. Nessuna edizione che copre la data → **il lotto non scade** (scelta conservativa, lato membro). | APERTA |
| Q-48 | `wallet-service/EditionCloseBatchService.java` | Un accredito STS che arriva mentre la chiusura dell'edizione è in corso: in quale edizione conta? | La chiusura blocca le righe `member_tier` (`FOR UPDATE`): un accredito già in volo viene visto dalla chiusura (conta per l'edizione che si chiude); uno arrivato dopo il lock attende il commit e si somma al `period_sts` azzerato (conta per la nuova edizione). Nessun punto viene perso. | APERTA |
| Q-49 | `ingestion-service/TransactionsController.java` | F-ING-07 prevede il reso (`purchase.returned`), ma il corpo di `POST /v1/transactions` in `ingestion-service.md §3` non ha un campo per distinguerlo, né un `id` per il reso. | Campo opzionale `kind` (`PURCHASE` predefinito, `RETURN`); il reso diventa `purchase.returned` con `id = "txn-return-" + orderId` (dati: `orderId`, `amount`). Stessa pipeline di `/v1/events`. | APERTA |
| Q-50 | `docs/12` (accettazione M3) | Il criterio sul gruppo esclusivo chiede `reason=EXCLUSIVE_GROUP`, mentre `docs/03 §3.5`, F-CMP-09 (`docs/02`) e BO-03 (`docs/08`) usano `EXCLUSIVE`. | Mantenuto `EXCLUSIVE` (fonti con precedenza maggiore; è anche il valore già nel registro valutazioni e nel fatto `campaign.evaluated`). Da allineare `docs/12`. | APERTA |
| Q-51 | `campaign-service/CampaignAdminService.update` | `docs/03 §3.6` limita ai campi sicuri solo le campagne `LIVE`; non dice nulla di `PAUSED`, `ENDED`, `ARCHIVED`. | Scelta conservativa: `PAUSED` bloccata come `LIVE` (`409 CAMPAIGN_LIVE_LOCKED`); `ENDED`/`ARCHIVED` non modificabili (`409 CAMPAIGN_NOT_EDITABLE`); `code` mai modificabile (`409 CODE_IMMUTABLE`). | APERTA |
| Q-52 | `reward-service/CouponController` (BO-12) | BO-12 cita l'azione *Annulla* (`coupon.void`) e la cassa simulata *Segna come usato*, ma la matrice permessi di `docs/08 §2` non elenca né `coupon.void` né l'uso alla cassa. | `coupon.void` ai ruoli di `redemption.handle` (ADMIN, CARE), perché annullare un codice già emesso toglie un premio al membro; uso alla cassa a ogni ruolo operativo (ADMIN, MARKETING, LEGAL, CARE), mai ANALYST. Da aggiungere alla matrice di `docs/08 §2`. | APERTA |
| Q-53 | `docs/05 §5`, `contracts/events/` | `reward-service.md §4` dice che reward produce `reward.status.changed`, ma il catalogo dei fatti di `docs/05 §5` non lo elenca (c'è solo `campaign.status.changed`, EVT-FACT-11). | Aggiunti schema ed esempio `fact.reward.status.changed` con la stessa forma di `campaign.status.changed` (`rewardCode, name, previousStatus, newStatus`): è un nuovo `type` su un topic esistente, ammesso da `CLAUDE.md §1.5`. Da assegnare un ID EVT-FACT in `docs/05`. | APERTA |
| Q-54 | `wallet-service/RedemptionPayments.refund` | Il rimborso di una richiesta annullata (F-RWD-07) restituisce i punti, ma nessuna fonte dice cosa fare se nel frattempo il lotto da cui erano stati spesi è scaduto. | I punti tornano nei lotti d'origine ancora validi; quelli di lotti scaduti tornano in un lotto nuovo con la scadenza di un accredito di oggi (il membro non perde punti per un annullo che non ha deciso lui). Movimento `REFUND` unico con l'importo speso. | SUPERATA da Q-160 (docs/03 §4.2 prevale) |
| Q-55 | `reward-service/RedemptionTimeoutJob` | `reward-service.md §5` vuole il timeout delle richieste "ogni minuto", ma i job schedulati sono spenti in demo (`loyaltyhub.jobs.enabled=false`, ADR-024) e l'accettazione M4 richiede il timeout anche nella demo. | Il timeout è una rete di sicurezza della saga, non un job di calendario: ha un suo interruttore (`loyaltyhub.reward.redemption-timeout.enabled`, acceso di default) e gira anche in demo; in più `POST /v1/demo/jobs/timeout-redemptions?asOf=` per provarlo da BO-30. | APERTA |
| Q-56 | `gamification-service` · meccanica del concorso | La spec del servizio (§2) elenca `WHEEL, SCRATCH, BOX`; docs/02, docs/08 (BO-14), docs/09 (PT-06) e docs/10 (`IW-ESTATE`) usano `GIFT`. | Vince la fonte a precedenza più alta (`docs/servizi`, CLAUDE.md §6): valore `BOX` in API, seed e UI (etichetta "Pacco regalo"); `seed/contests.json` usa quindi `BOX` per `IW-ESTATE`. Se si preferisce `GIFT`, basta allineare la spec del servizio: il valore non è un ID né un contratto evento. `// SPEC-GAP: Q-56` in `Contest.MECHANICS`. | APERTA |
| Q-57 | `seed/contests.json` · periodo di `IW-ESTATE` | docs/10 §6 fissa `IW-ESTATE` al 1 lug – 31 ago, ma i seed usano date relative (§1.2) e un concorso `ENDED` con date future (demo accesa tra gennaio e agosto) sarebbe incoerente. | `@today-85d` → `@today-24dT23:59` (62 giorni, sempre concluso; oggi coincide col 1 lug – 31 ago). `IW-NATALE` resta 1–24 dic dell'anno corrente (`@soy+11M`). | APERTA |
| Q-58 | `seed/gamification-history.json` | docs/10 §6 vuole per `IW-ESTATE` 142 vincite di 18 membri "con nomi di fantasia oltre ai 12", ma ogni `memberId` dei seed deve esistere in `members.json` (docs/10 §11.2) e l'anagrafica è di member-service. Inoltre le vincite storiche (e quella di Matteo su `IW-AUTUNNO`) non hanno un movimento nel ledger del wallet seminato. | Vincitori solo tra i membri esistenti (10, 142 vincite, 3 giocate perdenti per vincita); saldi del wallet invariati (lo storico di gioco è solo nel servizio gamification). Per 18 vincitori servirebbero 6 membri in più in `members.json`: decisione sui dati demo. | APERTA |
| Q-59 | `gamification-service` · periodo `EDITION` degli obiettivi | Le edizioni sono di wallet-service (`editions.json`) e gamification non può leggerle in modo sincrono; `docs/03 §8` non dice come ricavare la chiave. | Chiave `ED-<anno>` dal giorno dell'azione (Europe/Rome): coincide con le edizioni del seed, tutte anno solare. Con edizioni non annuali servirebbe consumare `edition.*` o uno snapshot. `// SPEC-GAP: Q-59` in `AchievementRules.periodKey`. | APERTA |
| Q-60 | `seed/gamification-history.json` · classifiche | docs/10 §6 chiede, oltre ai membri attivi, "15 nickname di fantasia per riempire le top 10", ma i punteggi sono per `memberId` e ogni membro dei seed deve esistere in `members.json` (stessa questione di Q-58). | Punteggi solo per i membri esistenti: 9 attivi nel mese (più Roberto, bloccato, per mostrare l'esclusione), 8 con STS nell'edizione; la riga "Sei 14°" fuori dalla top N resta implementata ma non si vede coi dati attuali. Da decidere insieme a Q-58 (membri aggiuntivi in `members.json`). | APERTA |
| Q-61 | `member-service` (referral) · BO-17 · PT-11 | (a) Il codice amico di un membro non `ACTIVE` (bloccato, anonimizzato) non è trattato da nessuna fonte. (b) L'imbuto di BO-17 (invitato → registrato → prima azione qualificante → premiato) ha passi che member-service non conosce: gli inviti inviati non sono tracciati e il premio è deciso dal motore campagne. (c) PT-11 mostra "n di 10 inviti premiati in questa edizione", ma member-service non conosce le edizioni. | (a) Codice di un membro non `ACTIVE` → `422 REFERRAL_CODE_INVALID` (nessun premio a un membro sospeso). (b) Imbuto coi dati disponibili: invitati = registrati con codice, premiati = completati (le campagne `CMP-REFERRAL-*` premiano ogni completamento entro i propri limiti), con nota in pagina. (c) Completamenti dell'anno solare (le edizioni del seed sono annuali, come Q-59), tetto dal `memberLimit` di `CMP-REFERRAL-REFERRER`. `// SPEC-GAP: Q-61` in `MemberService.resolveReferral` e `web/lib/member/referral.ts`. | APERTA |
| Q-62 | `gamification-service/InstantRepository.plantLast` | `gamification-service.md §3` pianta l'istante a `now − 1 s`, ma il claim prende l'istante aperto più vecchio (docs/03 §6) e il seed ha istanti già maturati e non riscossi: la giocata successiva vincerebbe un altro premio, contro BO-14 ("la prossima giocata vince" il premio scelto). | L'istante piantato va a `min(now − 1 s, istante aperto più vecchio − 1 s)`: resta nel passato e diventa il primo del claim. Il montepremi non cambia. `// SPEC-GAP: Q-62`. | APERTA |
| Q-63 | `seed/scenarios.json` · `SCN-ONBOARDING` | Il passo "completa profilo (`member.profile.completed`)" di docs/10 §8 non è un'azione esterna: nasce dal member-service al salvataggio del profilo, e gli scenari (ingestion) non possono chiamare altri servizi (CLAUDE.md §1.3). | Passo simulato: azione `member.profile.completed` dalla fonte `simulator` → `CMP-PROFILE` +150. Il profilo di Anna in PT-08 resta incompleto (manca la città); completarlo dopo dal portale emette il fatto ma `CMP-PROFILE` (1 / sempre) non paga due volte. | APERTA |
| Q-64 | `seed/message-templates.json`, `notification-rules.json` | docs/10 §7 vuole per Sofia un messaggio non letto "richiesta confermata", ma nessuno degli 11 `MSG-*` minimi (+ `MSG-BIRTHDAY`) lo copre. | Aggiunti `MSG-REWARD-CONFIRMED` e la regola `NR-REWARD-CONFIRMED` su `reward.redemption.confirmed`. Effetto: un premio con coupon automatico dà due messaggi ("confermata", poi "pronto"). | APERTA |
| Q-65 | `seed/notification-rules.json` · `NR-POINTS-EARNED` | Il wallet emette `wallet.points.earned` anche per gli STS: senza filtro un acquisto darebbe "162 punti" e "130 punti". | Condizione `data.currency = PTS` sulla regola. | APERTA |
| Q-66 | `engagement-service/TemplateEngine` | docs/10 §7 usa segnaposto senza radice (`{{amount}}`, `{{campaignName}}`), la scheda servizio §5 (precedenza maggiore) vuole `{{data.…}}` sul contesto `{data, member, event}`. | Seguita la scheda servizio: `{{data.amount}}`; `campaignName` non è nel fatto e i template seed non lo usano. | APERTA |
| Q-67 | `engagement-service/InboxService` | `EMAIL_FAKE` "produce solo un'anteprima consultabile da BO-19": non è detto se compaia anche nell'inbox del portale. | Solo nel registro `/v1/messages`: niente inbox del portale né conteggio dei non letti. | APERTA |
| Q-68 | `engagement-service/MessageSendHandler` | Chiave di deduplica di un messaggio da effetto `message.send` non specificata. | `data.effectId` (in mancanza, l'id dell'evento); il contesto `data.*` del template è l'effetto con `params` sovrapposti. | APERTA |
| Q-69 | `engagement` V1 · `notification_rule` | La scheda servizio elenca solo `id`; docs/06 §2 vuole `id` e `code` per le entità di configurazione. | Colonna `code` univoca in più; i path accettano id o codice. | APERTA |
| Q-70 | `engagement-service/InboxService` | Nessuna fonte dice se i membri non attivi ricevono messaggi. | Scelta conservativa: nessun messaggio agli `ANONYMIZED`; i `BLOCKED` li ricevono (l'assistenza può doverli informare). | APERTA |
| Q-71 | `seed/contents.json` · pop-up | docs/10 §7 vuole `POP-WELCOME` per gli "iscritti da < 7 giorni" e `POP-WEEKEND` "solo sab–dom", ma il pubblico della scheda servizio è solo `{tiers, segments, statuses}`; `CNT-BLACK-FRIDAY` è "SCHEDULED", stato che non esiste tra quelli dei contenuti. | Nel seed: chiavi aggiuntive `audience.registeredWithinDays` e `audience.daysOfWeek` (conservate dal backend, applicate ai pop-up con M6.2); Black Friday `LIVE` con inizio il 27 nov (`@soy+10M+26d`): fuori calendario fino ad allora. | APERTA |
| Q-72 | `engagement-service/ContentSelection.LIMITS` | docs/03 §9 fissa solo `HOME_HERO` 1 e `HOME_GRID` 6. | `CATALOG_TOP` 1 (un banner), `CONTEST` 3, `WIN` 1 (la card del premio vinto). | APERTA |
| Q-73 | `campaign-service/CampaignEngine` | Un `SEND_MESSAGE` senza `templateCode` (o con `params` non oggetto) non è previsto; campaign non può verificare che il template esista (è di engagement). | La campagna intera è scartata col motivo `EFFECT_NOT_SUPPORTED_YET` (niente punti "a metà"); la validazione di gestione lo impedisce già in creazione. | APERTA |
| Q-74 | `web/lib/messages/templates.ts` (BO-19) | Il servizio accetta qualunque `link_target`, ma PT-12 segue solo percorsi del portale. | L'editor accetta solo link `/portal…`. | APERTA |
| Q-75 | BO-19 `TemplatesTab` | `POST /v1/message-templates/{code}/render` richiede un template già salvato. | Una bozza mai salvata mostra il testo grezzo invece dell'anteprima renderizzata. | APERTA |
| Q-76 | portale · `InboxBell` | docs/09 chiede l'arrivo in tempo reale delle notifiche, ma il portale ascolta l'SSE solo durante l'attesa di un'azione del membro. | Campanella aggiornata ogni 30 s più gli eventi dell'attesa: i messaggi nati altrove (scadenze, azioni da BO-28) arrivano entro 30 s. | APERTA |
| Q-77 | `seed/message-templates.json` · BO-19 | I seed non hanno template `EMAIL_FAKE`: il registro "anteprima e-mail" resta vuoto finché non se ne crea uno. | Nessun seed aggiunto; da decidere se aggiungere un template e una regola `EMAIL_FAKE` di esempio. | APERTA |
| Q-78 | BO-19 · "dove si vede" | `GET /v1/campaigns` non espone gli effetti né un filtro per template. | BO-19 legge il dettaglio di ogni campagna (≈20 letture, in cache 60 s); un filtro `?templateCode=` lato campaign lo eviterebbe. | APERTA |
| Q-79 | engagement `PUT /v1/theme` · BO-20 | La scheda chiede "contrasto testo/primario ≥ 4.5" senza dire quale testo; BO-20 chiede anche i nomi delle valute, assenti nella tabella `theme`. | Testo = `night` (è il colore del testo sulle superfici `primary` del portale), verificato anche su `bg`; colonne in più `currency_names` (default `punti`/`punti status`) e `version` per il lock ottimistico. Il primario usato come colore di testo su bianco (link) non è verificato, come nel tema Aurora. | APERTA |
| Q-80 | member `ActionLabels` · `SCN-DIGITAL` | Nessuna fonte dice chi imposta le etichette, ma SEG-DIGITAL è definito da etichette e SCN-DIGITAL deve portarle a Marco. | Le azioni `ebill.activated` e `directdebit.activated` aggiungono le etichette `ebill`/`directdebit` (mai le tolgono) ed emettono `member.updated`. | APERTA |
| Q-81 | member `SegmentRefresher` · `MemberSeeder` | Dopo il reset demo gli altri servizi azzerano gli snapshot in sequenza: un solo annuncio delle appartenenze può andare perso. | Dopo seed/reset si riemette `entered` per ogni appartenenza, di nuovo dopo `reannounce-delay-ms` (15 s), più `left` per quelle precedenti al reset; consumatori idempotenti. Va oltre "solo le differenze", ma solo al reset. | APERTA |
| Q-82 | member V2 · `member_activity_day` | Le finestre mobili a 30 e 90 giorni dei criteri richiedono contatori giornalieri. | Tabella interna `member_activity_day` non elencata nella scheda servizio §2. | APERTA |
| Q-83 | `seed/segments.json` · docs/10 §3 | docs/10 attende 4 membri in SEG-TORINO, ma nei seed solo Davide abita a Torino. | Città invariate; membri attesi = solo Davide. | APERTA |
| Q-84 | `seed/segments.json` · `seed/rewards.json` | docs/10 dice che SEG-VIP-EVENT è usato da `RWD-PLATINUM-EVENT`, ma il premio non lo cita. | Premio invariato: il segmento risulta "usato da nessuno" in BO-04. | APERTA |
| Q-85 | member `SegmentFactsRepository` | Nessuna fonte dice quali stati del membro si valutano nei segmenti dinamici. | Tutti tranne ANONYMIZED; i segmenti dei seed che vogliono solo i membri attivi dicono `member.status eq ACTIVE`. | APERTA |
| Q-86 | `seed/activity-history.json` | docs/10 assegna `activity-history.json` a campaign, ma il file non esisteva. | Creato con la sola sezione `memberActivity` letta da member; campaign potrà aggiungere i suoi contatori. | APERTA |
| Q-87 | member `SegmentCriteria` · `SegmentsController` | Piccole aggiunte non previste dalla scheda servizio. | I criteri accettano anche `actions.<type>.total`; criteri vuoti non includono nessuno; nuovo `GET /v1/segments/{id}/members` in lettura per BO-04. | APERTA |
| Q-88 | member `SegmentService` | La specifica non dice nulla sull'archiviazione di un segmento. | Archiviare emette `left` per tutti i membri; codice e tipo non si cambiano dopo la creazione (409 `SEGMENT_IMMUTABLE_FIELD`). | APERTA |
| Q-89 | ingestion `EventTypeService` · BO-09 | Nessuna fonte fissa il formato del codice di un tipo custom né chi modifica un tipo di sistema. | Codice minuscolo a punti come i tipi di sistema (2–4 parti, es. `meter.reading.sent`); categorie custom TRANSACTION/ENGAGEMENT/SERVICE; un tipo di sistema si modifica (nome, descrizione, icona, abilitazione) solo con ADMIN (`program.config`), un custom con `actiontype.custom`. | APERTA |
| Q-90 | BO-06 `ConditionBuilder` · motore campagne | docs/08 chiama `not` "NESSUNA" (nessuna regola vera), ma il motore lo valuta come "non tutte vere insieme". | Etichetta della specifica mantenuta; avviso quando un gruppo NESSUNA ha più di una regola, con il suggerimento di annidarvi un gruppo ALMENO UNA. Da decidere se cambiare il motore o l'etichetta. | APERTA |
| Q-91 | BO-06 · confronti sulle date | Il motore confronta `gt/gte/lt/lte/between` solo tra numeri: sulle date ISO funziona solo `eq`. | I comparatori sulle date restano proposti dal costruttore; da decidere se il motore debba confrontare le date ISO come testo. | APERTA |
| Q-92 | BO-06 · catalogo dei campi | docs/08 cita `campaign GET /v1/meta/condition-fields`, che non esiste. | Campi `member.*`, `context.*`, `history.*` fissi nel frontend secondo docs/03 §3.3; `data.*` da ingestion `/fields`, attributi da member. | APERTA |
| Q-93 | member `AttributeService` | La scheda non dice cosa succede ai valori quando si toglie o si cambia tipo a un attributo. | Una chiave con valori sui membri non si toglie né cambia tipo (`409 ATTRIBUTE_IN_USE`): nessun valore perso o reinterpretato. | APERTA |
| Q-94 | BO-04 · definizioni degli attributi | docs/08 assegna gli attributi alla capacità `segment.write` ma a nessuna schermata. | Le definizioni si gestiscono in fondo a BO-04 (Segmenti); i valori nella scheda `segments` di BO-03. | APERTA |
| Q-95 | docs/12 §M6 · engagement §7.5 | L'accettazione M6 chiede i 5 punti di engagement §7, ma il quinto (webhook) appartiene a M7 secondo la scheda del servizio e docs/12 §M7.2. | Accettazione M6 lasciata aperta solo su quel punto; si chiude con M7.2. | CHIUSA (M7.2) |
| Q-96 | lh-common `ApprovalsController` · BO-21 | docs/06 §7 definisce solo `GET /v1/approvals?status=IN_REVIEW`; BO-21 ha anche «Inviate da me» e la scheda policy. Nell'hub i tre servizi condividono il server web. | `/v1/approvals` è un controller comune che aggrega le fonti del processo (una da sola, tre nell'hub; il web deduplica); in più `?submittedBy=` (oggetti inviati da un attore, in qualunque stato) e `/v1/approvals/policy`; storico in `GET /v1/<risorsa>/{id}/approval-history`. | APERTA |
| Q-97 | engagement `webhook`, `webhook_delivery` | La scheda elenca poche colonne; BO-23 e lo scheduler ne richiedono di più. | Aggiunti `code`, `version`, `updated_*` sul webhook; `payload`, `signature`, `member_id`, `test`, `duration_ms`, `error`, `last_attempt_at`, `claimed_until` sulle consegne. | APERTA |
| Q-98 | engagement · *Riprova* manuale | La scheda non dice come si comporta un nuovo tentativo manuale. | Un solo tentativo immediato con la stessa tabella dei ritenti; dopo `GAVE_UP` un fallimento torna `GAVE_UP` senza ripartire; ogni non-2xx (anche 3xx, i redirect non si seguono) è un fallimento. | APERTA |
| Q-99 | engagement · URL dei webhook | La scheda blocca gli indirizzi privati solo nel profilo `free`, ma l'hub ospitato gira con `demo,inproc`. | Indirizzi privati e loopback bloccati in ogni profilo tranne `local` (al salvataggio e dopo la risoluzione DNS all'invio); proprietà per cambiarlo. Il DNS rebinding tra controllo e invio non è coperto. | APERTA |
| Q-100 | engagement · evento di prova | La scheda non dice cosa contiene l'evento di prova. | Esempio del contratto del primo tipo di fatto sottoscritto, con id, tempo e correlazione nuovi e `lhactor` di chi invia; esempi nel jar (`webhook-samples/`), Dockerfile con `COPY contracts/`. | APERTA |
| Q-101 | engagement · scheduler dei webhook in demo | In demo i job pianificati sono spenti, ma i webhook devono partire. | Interruttore proprio `loyaltyhub.webhooks.dispatcher.enabled` (acceso di default), invii su thread virtuale dedicato. | APERTA |
| Q-102 | `seed/webhooks.json` | docs/10 chiede un solo webhook disabilitato: il registro di BO-23 sarebbe vuoto. | 5 consegne storiche (3 `OK`, 2 `GAVE_UP`) firmate con un segreto di prova. | APERTA |
| Q-103 | engagement · API in più | BO-23 e i test richiedono endpoint non elencati. | `GET /v1/webhook-deliveries/{id}`, job demo `POST /v1/demo/jobs/deliver-webhooks?asOf=` (ADMIN); il test risponde 201 con la consegna; elenco consegne paginato. | APERTA |
| Q-104 | insight/ingestion · riprocessa | «Re-invio con lo stesso id» si scontra con la deduplica (source, id) di ingestion: tornerebbe `DUPLICATE`. | Intestazione opzionale `X-LH-Reprocess` (solo ADMIN) su `POST /v1/events`: ripubblica l'evento accettato così com'era; i consumer che l'avevano già elaborato lo saltano per idempotenza. | APERTA |
| Q-105 | BO-27 · stato iniziale | docs/08 chiama lo stato iniziale `NEW`, la scheda di insight `OPEN`. | API con `OPEN`, interfaccia «Nuova». | APERTA |
| Q-106 | insight · tracciato FAILED | «FAILED se esiste una voce DLQ» non distingue le voci chiuse. | Voci `OPEN` o `DISCARDED` → FAILED; `REPROCESSED` no (un nuovo fallimento apre una nuova voce). | APERTA |
| Q-107 | insight · seconda chiusura | La scheda non dice cosa risponde una seconda chiusura di una voce. | `409 DLQ_NOT_OPEN`, voce invariata. | APERTA |
| Q-108 | `SCN-POISON` · docs/10 | docs/10 dice sia «fallisce 3 volte» sia «eccezione non ritentabile». | Non ritentabile: un solo tentativo (`DEMO_POISON`, solo col profilo demo). | APERTA |
| Q-109 | insight · audit di riprocessa e scarta | insight «non produce eventi su Kafka», ma le scritture da backoffice vanno in audit (F-AUD-01). | Riprocessa e scarta pubblicano solo la voce di audit su `lh.audit.v1`. | APERTA |
| Q-110 | insight · nota allo scarto | §5 chiede la nota solo per effetti e fatti scartati. | Nota obbligatoria per ogni scarto. | APERTA |
| Q-111 | insight · `event_store` e DLQ | La scheda mette la famiglia DLQ in `event_store`, ma un record DLQ ha lo stesso `event_id` dell'originale. | I record DLQ vivono in `dlq_entry` e nel flusso live, non in `event_store`. | APERTA |
| Q-112 | campaign · reward · gamification · «versioni» (M7.6) | docs/12 chiede «versioni» per campagne, premi e concorsi senza dire se si intende uno storico delle revisioni. | Blocco ottimistico: il `PUT` porta la `version` letta, 409 `VERSION_CONFLICT` se superata, avviso «ricarica / sovrascrivi» nell'editor e «vN» in intestazione. Nessuno storico delle revisioni (lo storico delle transizioni c'è già da M7.1). | APERTA |
| Q-113 | gamification · codice della copia di un concorso | La scheda fissa `<code>-COPY-n` solo per le campagne (i premi usano `-COPY`). | Stessa regola delle campagne, troncando il codice a 40 caratteri; copia in DRAFT con premi a quantità piena, seme dal nuovo codice, istanti da generare, periodo dell'originale da correggere. | APERTA |
| Q-114 | ingestion · riprova su un evento già accettato altrove | Se nel frattempo un evento con la stessa fonte e lo stesso id è stato accettato, la scheda non dice cosa succede alla riga riprovata. | La riga diventa `DUPLICATE` con una spiegazione; niente ripubblicazione. | APERTA |
| Q-115 | ingestion · finestra dell'abbinamento automatico | F-ING-04 non limita quanti e quali eventi parcheggiati si riabbinano alla registrazione. | Ultimi 7 giorni (la finestra di pulizia) e al massimo 100 righe per registrazione. | APERTA |
| Q-116 | ingestion · esito dell'abbinamento automatico | Cosa fare se il riabbinamento automatico non porta ad ACCEPTED (membro non attivo, fonte spenta). | La riga resta `UNMATCHED` per l'operatore; si tocca solo se l'esito è ACCEPTED. | APERTA |
| Q-117 | ingestion · audit di riprova e abbina | §4 elenca l'audit solo per fonti, tipi e mapping. | Riprova e abbina vanno in audit come TRANSITION con l'attore; l'abbinamento automatico come job. | APERTA |
| Q-118 | ingestion · riga aggiornata o nuova | La scheda non dice se riprova/abbina creano una nuova riga. | Si aggiorna la riga esistente (subject originale conservato, payload = azione pubblicata); chi/come/quando nelle nuove colonne `resolution`, `resolved_by`, `resolved_at`. | APERTA |
| Q-119 | ingestion · quando scatta l'abbinamento automatico | F-ING-04 dice «alla registrazione del membro». | Solo su `member.registered` e solo per subject `external:`/`email:` (mai `member:<id>`). | APERTA |
| Q-120 | member · segnaposto dell'anonimizzazione | docs/03 dice «nome → Membro anonimo» senza indicare il campo. | Il segnaposto va nel `nickname`; nome e cognome diventano null, così il nome mostrato è «Membro anonimo». Stessa regola sul seed MBR-000012. | APERTA |
| Q-121 | member · attributi personali | Le definizioni degli attributi non dicono quali sono dati personali. | Si cancellano tutti gli attributi e i consensi; restano etichette, codice invito e legame di referral. | APERTA |
| Q-122 | lh-common · chiavi personali | Manca l'elenco dei campi da considerare dati personali nei payload. | `PersonalData.KEYS`: firstName, lastName, fullName, nickname, email, emailLower, phone, mobile, birthDate, gender, city, address, street, zip, postalCode, externalId, shipping, attributes, consents, avatarSeed. | APERTA |
| Q-123 | reward · spedizione di una richiesta ancora da evadere | Anonimizzare un membro con un premio fisico in attesa di spedizione. | L'indirizzo si cancella comunque; le note dell'operatore restano, col nome sostituito. | APERTA |
| Q-124 | gamification · nota di consegna | La nota di consegna di una vincita è testo libero e gamification non conosce il nome reale. | La nota diventa null. | APERTA |
| Q-125 | engagement · messaggi già resi | engagement conosce solo il nome di battesimo. | Si sostituisce solo quello nei messaggi già presenti in inbox; i payload webhook del membro sono ripuliti e rifirmati. | APERTA |
| Q-126 | insight · copie archiviate | Mascherare in lettura o riscrivere le copie in `event_store`, audit e DLQ. | Si riscrivono una volta per fatto di anonimizzazione; un evento con dati personali arrivato dopo entrambi i fatti non verrebbe ripulito. | APERTA |
| Q-127 | wallet · rettifiche su membri non attivi | F-MBR-04 non dice se le rettifiche manuali valgono per i membri non attivi. | Rifiutate (409 `MEMBER_ANONYMIZED`) solo per gli anonimizzati; gli altri stati non attivi restano rettificabili. | APERTA |
| Q-128 | ingestion · eventi dopo l'anonimizzazione | Senza e-mail ed externalId nell'indice, un evento successivo con quei riferimenti non trova il membro. | Esito `UNMATCHED` (con `member:<id>` resta `REJECTED/MEMBER_NOT_ACTIVE`). | APERTA |
| Q-129 | seed · `SCN-BAD-EVENT` · fonte disabilitata | docs/10 vuole un passo con «fonte disabilitata» ma le fonti del seed sono tutte abilitate (docs/10 §fonti). | Il passo usa una fonte sconosciuta (`pos-legacy`), che la pipeline respinge con lo stesso codice `SOURCE_DISABLED`; nessuna fonte aggiunta al seed. | APERTA |
| Q-130 | ingestion · scenari ripetibili con duplicato | docs/10 chiede lo stesso `id` due volte in `SCN-DUPLICATE`, ma con un id fisso la seconda esecuzione darebbe due DUPLICATE. | Un `eventId` di passo può contenere `{run}`, sostituito con l'id dell'esecuzione: duplicato dentro l'esecuzione, scenario ripetibile. | APERTA |
| Q-131 | lh-common · hub · ritentativi dei consumer | docs/04 dice «3 tentativi con backoff 1 s / 5 s / 15 s» (tre ritardi = quattro tentativi); docs/12 (accettazione M0) vuole la DLQ «dopo 3 tentativi». | Vince il conteggio: 3 tentativi con ritardi 1 s e 5 s, configurabili con `loyaltyhub.consumer.retry-backoff-ms` (`LH_CONSUMER_RETRY_BACKOFF_MS`); il bus in-process dell'hub usa la stessa proprietà. | APERTA |
| Q-132 | web · keep-alive dopo lo stop | docs/07 §8 dice solo «si ferma dopo 45 min senza interazione». | La prossima interazione (pointerdown, keydown, wheel) riavvia il ciclo di 4 minuti, senza una sveglia immediata. | APERTA |
| Q-133 | web · ingressi con stato sconosciuto | Gli ingressi si attivano coi 4 servizi core UP; non è detto cosa fare se lo stato non è noto. | Stato in caricamento o `/api/demo/status` in errore → ingressi bloccati. | APERTA |
| Q-134 | web · frequenza del controllo di stato | docs/07 fissa il polling a 3 s solo dopo «Accendi la demo». | Altrimenti ogni 8 s; nessun controllo mentre il keep-alive è fermo per inattività (i controlli terrebbero svegli i servizi), con una nota di pausa. | APERTA |
| Q-135 | web · avviso Kafka giù da oltre 2 minuti | Quale stato conta per l'avviso. | Solo `DOWN` reale; `SLEEPING` (ingestion irraggiungibile) lascia ignoto lo stato di Kafka e non conta. | APERTA |
| Q-136 | web · schede membro nel Demo Hub | docs/07 dice «12 schede», ma le persone selezionabili escludono il membro anonimizzato (MBR-000012). | Si mostrano i membri restituiti da `/v1/demo/personas` (11 su una demo appena azzerata). | APERTA |
| Q-137 | web · BO-03 membro INACTIVE | docs/08 non prevede *Riattiva*. | Per un membro INACTIVE *Blocca* e *Disattiva* sono disabilitati (Blocca → Sblocca lo riattiverebbe di fatto). | APERTA |
| Q-138 | member · motivo del cambio stato | Non è detto se il motivo sia obbligatorio; l'API lo accetta facoltativo. | Facoltativo; inviato solo se compilato (finisce in audit e nel fatto). | APERTA |
| Q-139 | contratti · stati del membro | `contracts/events/fact/member.{status.changed,registered,updated}` ammettevano `ACTIVE, SUSPENDED, BLOCKED, CLOSED, ANONYMIZED`; docs/02 F-MBR-04, docs/03 §membro e member-service usano `INACTIVE`: un membro disattivato produceva un fatto fuori contratto. | **Deciso da Giuseppe (2026-09-24):** `INACTIVE` aggiunto agli enum dei tre contratti (allargamento compatibile, nessuna rinomina né rimozione). | DECISA |
| Q-140 | wallet `WalletService.applyGrant` · TB-WAL-GRT-049…051 | docs/03 §2 dice «solo i membri ACTIVE accumulano», ma il filtro sullo stato è del motore (§3.5): non è detto se un `points.grant` già deciso per un membro che nel frattempo è `BLOCKED`, `INACTIVE` o `ANONYMIZED` vada applicato. | Oggi il wallet accredita comunque (anche a membri `BLOCKED` e `ANONYMIZED`): **non è la scelta conservativa**. Proposta, non implementata: il wallet non accredita i membri `BLOCKED`/`ANONYMIZED` (effetto scartato con log e voce di audit), e continua ad accreditare gli `INACTIVE` il cui effetto è stato deciso prima del cambio di stato. | APERTA |
| Q-141 | wallet `WalletService.applyGrant` · TB-WAL-GRT-061, GRT-065 | Il contratto `effect.points.grant` non dice cosa fare di un effetto senza `effectId` o con un `subject` che non è un membro. | Ignorato con un log, senza movimento, errore né DLQ (nessun punto accreditato: conservativo). Alternativa: inviarlo in `lh.dlq.v1` per renderlo visibile in BO-27. | APERTA |
| Q-142 | wallet `ExpiryPolicy` · TB-WAL-POL-013, POL-023, POL-024 | Nessuna fonte dice cosa fare con una policy `ROLLING_MONTHS` senza `months`, con una valuta senza policy o con un tipo di policy sconosciuto. | `months` assente = 12 (il valore del seed); valuta senza policy e tipo sconosciuto = il lotto non scade (conservativo: nessun punto del membro sparisce per un dato di configurazione mancante). | APERTA |
| Q-143 | wallet `WalletService` · TB-WAL-TUP-016, TUP-017 | docs/03 §4.3 fa salire di livello «dopo ogni accredito STS», ma non dice se gli STS in attesa (`pendingDays` > 0) contano nel `periodSts` all'accredito o al rilascio. | Contano al rilascio: nessuna salita finché il lotto è `PENDING` (conservativo: si sale solo con STS già disponibili). | APERTA |
| Q-144 | wallet `RedemptionPayments.spend` · TB-WAL-SPD-018 | EVT-FACT-22 elenca i motivi di rifiuto senza una precedenza: membro non attivo e saldo insufficiente insieme. | Prevale `MEMBER_NOT_ACTIVE` (lo stato si controlla prima del saldo). | APERTA |
| Q-145 | wallet `RedemptionPayments.spend` · TB-WAL-SPD-021…023 | Il contratto `reward.redemption.requested` non dice cosa fare di una richiesta senza `redemptionId` o con `pointsCost` 0, né di un membro senza wallet. | Richiesta incompleta o a costo 0 ignorata (nessun movimento né fatto: la richiesta scade col timeout di reward); membro senza wallet → rifiuto `INSUFFICIENT_BALANCE` con `available` 0. Nessun punto speso: conservativo. | APERTA |
| Q-146 | wallet `WalletService.adjustBalance` · TB-WAL-ADJ-031, ADJ-032 | wallet §3 chiede una nota «≥ 10 caratteri» senza dire se contano gli spazi. | Si conta il testo senza spazi ai bordi (`trim`): una nota di soli spazi è rifiutata (conservativo). | APERTA |
| Q-147 | wallet `WalletService.adjustBalance` · TB-WAL-ADJ-042 | Nessuna fonte dice cosa fare di una rettifica su un membro di cui il wallet non ha ancora il wallet (fatto `member.registered` non arrivato o id sbagliato). | Oggi il wallet viene creato e la rettifica applicata: **non è la scelta conservativa** (un id digitato male riceve punti). Proposta, non implementata: `404` se il membro non ha wallet, come la vista `GET /v1/wallets/{id}`. | APERTA |
| Q-148 | wallet `PointsLotRepository.consumeFifo` · TB-WAL-ADJ-043 | docs/03 §4.2 fissa l'ordine di consumo per la spesa, non per un addebito manuale. | Stesso ordine della spesa (prima i lotti che scadono prima). | APERTA |
| Q-149 | wallet `EditionCloseRule` · TB-WAL-CLR-033 | La regola di chiusura (docs/03 §4.3) non dice cosa fare di un membro con un livello che non esiste più nella scala. | Oggi il livello sconosciuto è trattato come il primo della scala: il membro passa a BASE con esito `RETAINED`. **Non del tutto conservativo** (una discesa non dichiarata come tale). Proposta, non implementata: lasciare il membro invariato e segnalarlo nel riepilogo della chiusura. | APERTA |
| Q-150 | wallet `EditionsController` · TB-WAL-ECL-006 | wallet §3 riserva ad `ADMIN` solo l'applicazione della chiusura; per l'anteprima non indica un ruolo. | Anteprima con qualsiasi ruolo (sola lettura). | APERTA |
| Q-151 | wallet `EditionService` · TB-WAL-EDN-007 | docs/03 §4.4 dice «periodi contigui, senza sovrapposizioni», ma il servizio rifiuta solo le sovrapposizioni. | Oggi un buco tra due edizioni è accettato: **non è la scelta conservativa** (gli STS guadagnati nel buco non appartengono a nessuna edizione, vedi Q-47). Proposta, non implementata: `422 EDITION_NOT_CONTIGUOUS` se l'inizio non è il giorno dopo la fine dell'edizione precedente (o la fine non è il giorno prima della successiva). | APERTA |
| Q-152 | wallet `EditionService` · TB-WAL-EDN-010 | Nessuna fonte fissa l'errore per un codice di edizione già esistente. | `409` (conflitto sulla chiave). | APERTA |
| Q-153 | wallet `CurrencyService` · TB-WAL-CUR-006, CUR-007, CUR-009 | wallet §2 non fissa i limiti di `months` di `ROLLING_MONTHS`. | Da 1 a 60 mesi; fuori → `422`. | APERTA |
| Q-154 | wallet `TierAdminService` · TB-WAL-TAD-015 | F-TIER-01 non fissa un minimo per il moltiplicatore di livello. | Moltiplicatore 0 → `422` (un livello non può azzerare i punti). | APERTA |
| Q-155 | wallet `WalletQueryService.expiringSoon` · TB-WAL-WVW-007 | wallet §3 non dice se un lotto già scaduto ma non ancora spazzato dal job conti in `expiringSoon`. | Escluso: `expiringSoon` guarda solo le scadenze future entro 30 giorni. | APERTA |
| Q-156 | wallet `WalletJobsController` · TB-WAL-JOB-014 | wallet §3 dice «data di riferimento» senza fissare il formato di `asOf`. | Istante ISO oppure data pura; una data pura vale come ultimo istante di quel giorno in Europe/Rome (scadono anche i lotti di quel giorno). | APERTA |
| Q-157 | member · `POST /v1/members` senza guardia | docs/08 §2 riserva `member.write` ad ADMIN e CARE, ma member §3 usa lo stesso endpoint per la registrazione dal portale (F-MBR-06), che arriva come `ANALYST:anonymous`: una guardia romperebbe l'iscrizione. | Endpoint aperto (la UI del backoffice nasconde il pulsante). Proposta: `POST /v1/portal/members` per il portale e `@RequiresRole({ADMIN, CARE})` sul POST di gestione. | APERTA |
| Q-158 | campaign · nomi dei motivi di scarto (estende Q-50) | docs/12 M1 scrive `MEMBER_LIMIT_REACHED` e F-CMP-09 elenca `NOT_LIVE`; il motore emette `LIMIT` (docs/03 §3.5, campaign §7) e non può emettere `NOT_LIVE` (valuta solo le LIVE). | Restano i codici del motore (`NOT_IN_SCHEDULE, AUDIENCE, CONDITION, EXCLUSIVE, LIMIT, BUDGET`), già in `evaluation_log`, fatti e UI; da allineare il testo di docs/12 e F-CMP-09. | APERTA |
| Q-159 | gamification · periodi degli obiettivi | docs/03 §8 elenca `NONE, DAY, WEEK, MONTH, EDITION`; docs/08 BO-15 e docs/10 usano `MONTH/EDITION/EVER`, come il codice. | Restano `EVER/MONTH/EDITION` (DAY e WEEK → 422); EVER equivale a NONE di docs/03. | APERTA |
| Q-160 | wallet · scadenza del rimborso (supera Q-54) | docs/03 §4.2 chiede un lotto nuovo con scadenza `max(scadenza più lontana dei lotti consumati, oggi + 30 gg)`; Q-54 ripristinava i lotti d'origine. Non coperti: lotto consumato senza scadenza, spesa senza consumi registrati (storico del seed). | Lotto nuovo per l'intero importo come da docs/03; lotto consumato senza scadenza → rimborso senza scadenza; nessun consumo → oggi + 30 gg (`SPEC-GAP: Q-160` in `RedemptionPayments`). | APERTA |
| Q-161 | engagement · pubblico dei contenuti | engagement §2 (`{tiers[], segments[], statuses[]}`) e docs/03 §9 non dicono come si combinano i criteri; per le campagne (tier OR segmento) manca la regola scritta in docs/03 §3.2. | AND tra i criteri, OR dentro ciascuno (il più restrittivo: meno contenuti a chi non è target). | APERTA |
| Q-162 | seed · `personas.json` | docs/10 §1 prevede `seed/personas.json` letto dai servizi per i nomi in audit; il file non esiste. | Personas solo nel web (`web/lib/persona/personas.ts`, nomi di docs/10 §2); l'audit mostra `RUOLO:username`. | APERTA |
| Q-163 | wallet/campaign · moltiplicatore alla salita di livello | Con un acquisto che fa salire di livello, i PTS della stessa azione usano il moltiplicatore precedente solo perché nel seed l'effetto PTS precede quello STS; nessuna fonte fissa l'ordine. | Moltiplicatore del livello in vigore all'inizio dell'azione (comportamento del seed, docs/10 §4); da scrivere in docs/03 §4.2 e rendere indipendente dall'ordine degli effetti. | APERTA |
| Q-164 | ingestion · `POST /v1/sources` | ingestion §3 prevede anche la creazione di fonti; esiste solo `PUT /v1/sources/{code}`. | Fonti solo da seed; da BO-09 si accendono, spengono e se ne restringono i tipi. | APERTA |
| Q-165 | campaign · `cooldownMinutes` e `perMemberPoints` | F-CMP-05 e docs/03 §3.2 li prevedono senza definirne la semantica (per periodo? totale?). | `cooldownMinutes` = minuti dall'ultima attivazione del membro sulla campagna; `perMemberPoints` = tetto sulla vita della campagna → scarto `LIMIT`. | APERTA |
| Q-166 | campaign · ingestion · lh-common · pulizie | RNF-07 chiede di potare `evaluation_log` (> 30 gg), `inbound_event` (> 7 gg o > 20 000 righe), `processed_event` (> 14 gg); nessun job esiste. | Nessuna pulizia in demo (il reset riporta i volumi al seed); proposta: job notturni sotto `loyaltyhub.jobs.enabled`. | APERTA |
| Q-167 | insight · gamification · schede `timeline` e `game` di BO-03 | `GET /v1/members/{id}/timeline` (insight §3) e `GET /v1/members/{id}/gamification` (gamification §3) non esistono. | Le due schede degradano con lo stato vuoto. | APERTA |
| Q-168 | lh-common · `GET /v1/demo/info` | docs/06 §10 e BO-30 lo prevedono (versione, profili, conteggi, ultimo reset). | Assente; BO-30 non mostra lo stato per servizio. | APERTA |
| Q-169 | campaign · ritentativo NO_MEMBER | campaign §5: «ritenta 3 volte» se manca lo snapshot del membro; intervallo e meccanismo non specificati (nessun topic nuovo). | `NO_MEMBER` immediato. Proposta: eccezione ritentabile nel consumer (backoff di Q-131), poi `NO_MEMBER` invece della DLQ. | APERTA |
| Q-170 | engagement · estremi del calendario dei contenuti (TB-ENG CAL-001, CAL-004, END-005) | Né docs/03 §9 («in calendario») né engagement §5 («`end_at` passato») fissano cosa succede all'istante esatto di inizio e di fine. | Intervallo semiaperto `[startAt, endAt)`: visibile da `startAt` incluso, escluso a `endAt`; il job porta in `ENDED` un contenuto con `endAt` uguale all'istante del giro. È già l'opzione conservativa (a `endAt` il contenuto non si vede più). | APERTA |
| Q-171 | engagement · spareggio dell'ordine (TB-ENG ORD-002) | docs/03 §9 ordina per `priority` decrescente ma non dice come spareggiare due contenuti con la stessa priorità. | Codice crescente (ordine deterministico, lo stesso di BO-18 «Per posizione»). | APERTA |
| Q-172 | engagement · estensioni del pubblico Q-71 (TB-ENG EXT-005, EXT-016) | Con `registeredWithinDays` non è detto cosa fare se la data di iscrizione non è nota; Q-71 introduce le chiavi per i pop-up ma non dice se valgono per card e banner. | Iscrizione ignota ⇒ fuori dal pubblico; `registeredWithinDays` e `daysOfWeek` applicate a ogni tipo di contenuto. Opzione conservativa (restringono, non allargano il pubblico). | APERTA |
| Q-173 | engagement · transizioni con azione sconosciuta o assente (TB-ENG LIF-009/010, -019/020, -029/030, -039/040, -049/050) | docs/06 §2 dà `409` per la transizione non valida e `400` per i parametri errati: un'azione assente o inesistente può essere l'una o l'altra. | `409 INVALID_TRANSITION`, stato invariato. Nessun rischio sui dati (lo stato non cambia in entrambi i casi); proposta, da non implementare senza decisione: `400` per azione assente o fuori elenco, `409` solo per un'azione valida non ammessa dallo stato. | APERTA |
| Q-174 | engagement · vincoli di creazione e modifica dei contenuti (TB-ENG EDT-012, -017…019, -025, -033, -037…045, -047, -052, -055…057, -060) | Senza fonte: banner solo in `CATALOG_TOP`, `PRIZE` ⇔ `WIN`, `linkCode` obbligatorio, titolo ≤ 80 e testo ≤ 280, priorità 0…1000 con 50 di default, fine uguale all'inizio, CTA `http://`, codice minuscolo, codice e tipo immutabili, `ARCHIVED` non modificabile, `ENDED` modificabile, pop-up chiudibile di default; docs/03 §3.6 blocca i campi non sicuri solo sui `LIVE`. | Come nel codice (`422 CONTENT_INVALID` sui vincoli, `409 CODE_IMMUTABLE`/`KIND_IMMUTABLE`/`CONTENT_NOT_EDITABLE`, codice portato in maiuscolo). **Non conservativo**: un contenuto `ENDED` resta modificabile (`200`), mentre docs/17 US-E07-01 chiede `409 CONTENT_NOT_EDITABLE` come per campagne, premi e concorsi; e un `PAUSED` si modifica in ogni campo, mentre campaign (Q-51) lo blocca come un `LIVE`. Proposta, non implementata: `ENDED` → `409 CONTENT_NOT_EDITABLE`; `PAUSED` → stessi campi sicuri del `LIVE` (`409 CONTENT_LIVE_LOCKED`). | APERTA |
| Q-175 | engagement · duplicazione, risposta del portale, anteprima `WIN` (TB-ENG DUP-002, DUP-006, PRV-011, PRV-012) | engagement §3 non fissa il codice della copia né la forma della risposta del portale; BO-18 non dice quante card vincita mostrare in anteprima. | Copia `<CODICE>-COPIA`, poi `-COPIA-2`…, codice troncato a 40 caratteri, titolo "(copia)"; il portale riceve solo i campi di disegno (niente pubblico, stato, versione); l'anteprima `WIN` mostra tutte le card idonee, una per premio. Conservativo (il portale espone il minimo). Da notare: campaign usa `-COPY-n`, engagement `-COPIA-n`; proposta di uniformare solo con una decisione. | APERTA |
| Q-176 | engagement · ruoli senza ● in docs/08 §2 (TB-ENG ROL con LEGAL, CARE, intestazione non valida; WROL-002…004, -007…010) | `content.write` (ADMIN, MARKETING) e `webhook.write` (solo ADMIN) non sono marcate ●: non è prescritto che il backend rifiuti gli altri ruoli; né cosa valga un `X-LH-Actor` malformato. | `403 FORBIDDEN_ROLE` a ogni ruolo fuori matrice; intestazione non valida = `ANALYST` (sola lettura). Opzione conservativa. | APERTA |
| Q-177 | engagement · motore dei template (TB-ENG TPL-007, -008, -010, -014, -017, -027, -028, -030, -038…040, TPV-007) | engagement §5 non dice come trattare valori non semplici, indici di array, spazi nelle graffe, template nullo, HTML nei valori, cifre decimali e arrotondamento di `\|number`, ripieghi di `\|number`/`\|date` e formattatori sconosciuti. | Valore non semplice ⇒ vuoto; `arr.0.name` risolto; spazi ammessi; template nullo ⇒ stringa vuota e valido; nessun escape HTML; `\|number` a 2 decimali con arrotondamento al pari; valori non validi e formattatori sconosciuti ⇒ valore grezzo. L'assenza di escape è sicura solo finché i messaggi si mostrano come testo (inbox INAPP resa da React); proposta, non implementata: escape dei valori per ogni canale che renderà HTML. | APERTA |
| Q-178 | engagement · gestione di template, regole e webhook (TB-ENG TAD-004, TAD-010, TAD-014, RAD-012, WDLV-029) | engagement §2 non dà un canale di default al template né dice se nome e codice siano obbligatori/immutabili. | Canale `INAPP` di default; nome obbligatorio (`422`); codice immutabile (`409 CODE_IMMUTABLE`), come per contenuti e campagne. Opzione conservativa. | APERTA |
| Q-179 | engagement · condizioni sui dati (TB-ENG CND-041, -044, -045, -049, -063…065) | docs/03 §3.3 non dice se `null` vale come assente, se gli estremi di `between` sono inclusi, se una stringa numerica si confronta come numero, quanto valgono i gruppi vuoti, cosa fa un comparatore sconosciuto. | `null` = assente; `between` con estremi inclusi; stringa numerica convertita; `all` e `any` vuoti ⇒ vero; comparatore sconosciuto ⇒ falso (la gestione lo rifiuta al salvataggio). **Non conservativo**: `any` senza regole vale vero, quindi una regola di notifica con un gruppo `any` vuoto scatta sempre; proposta, non implementata: `any` vuoto ⇒ falso (o `422` al salvataggio). | APERTA |
| Q-180 | engagement · destinatari delle notifiche (TB-ENG RUL-002, -005, -012, -015) | Q-70 esclude solo gli `ANONYMIZED` (e tratta i `BLOCKED`); non dice nulla dei membri `INACTIVE` né di quelli di cui engagement non ha ancora lo snapshot. | Ricevono il messaggio. Per lo snapshot mancante è la scelta prudente (il fatto riguarda un membro esistente, lo snapshot può arrivare dopo). **Non conservativo** per gli `INACTIVE`: proposta, non implementata, nessun messaggio ai membri `INACTIVE`. | APERTA |
| Q-181 | engagement · tipo di fatto delle regole ed effetto `message.send` (TB-ENG RAD-002, DDP-014) | docs/05 usa i tipi completi, le regole la forma breve; docs/03 §3.4 non dice cosa fare di un `message.send` senza membro. | Tipo completo accettato e salvato in forma breve; `message.send` con `subject` non `member:` ⇒ nessun messaggio, effetto in DLQ con `lh-error-code` `INVALID_EFFECT`. Opzione conservativa. | APERTA |
| Q-182 | engagement · inbox (TB-ENG IBX-005, IBX-014) | PT-12 non dice cosa fa «segna letto» su un messaggio già letto; docs/06 §2 vuole il `memberId` esplicito ma non dice se nel corpo o nella query di `read-all`. | Idempotente (`200`, `readAt` invariato); `memberId` accettato anche nel corpo. | APERTA |
| Q-183 | engagement · formato del tema (TB-ENG THM-005…007, THA-012, THA-020…023) | engagement §3 dice «colori validati come esadecimali» senza la forma; F-THM-01 non fissa obbligatorietà e lunghezza del nome del programma né le forme ammesse del logo. | Solo `#RRGGBB` (rifiutati senza `#`, `#RGB`, `#RRGGBBAA`); nome del programma obbligatorio e ≤ 40 caratteri; logo solo percorso `/…` o `https://`. Opzione conservativa. | APERTA |
| Q-184 | engagement · URL dei webhook oltre Q-99 (TB-ENG WURL-025, -026, -029…032, -045…051) | Q-99 nomina privati e loopback; non gli altri intervalli speciali (CGNAT, 192.0.0/24, 198.18/15, multicast, 240/4), i nomi `.local`/`.internal`, le forme numeriche dell'host, credenziali, frammento, lunghezza. | Intervalli speciali e nomi `.local`/`.internal` rifiutati; credenziali e frammento rifiutati; URL fino a 500 caratteri. **Non conservativo**: un host in forma decimale (`2130706433` = 127.0.0.1) passa al salvataggio e si blocca solo alla risoluzione DNS dell'invio; proposta, non implementata: rifiutare al salvataggio gli host numerici non in forma puntata. | APERTA |
| Q-185 | engagement · ritenti e consegne dei webhook (TB-ENG WRTY-009, WRTY-020, WDLV-013, WDLV-017, WDLV-021, WDLV-023, WDLV-031) | engagement §5 e Q-98 non coprono: tentativo numero 0, *Riprova* su una consegna `PENDING` o già in invio, evento di prova su un webhook disattivato, webhook senza tipi di fatto o senza nome. | Tentativo 0 ⇒ `GAVE_UP`; *Riprova* su `PENDING` ⇒ `409 DELIVERY_NOT_RETRYABLE`, durante un invio ⇒ `409 DELIVERY_BUSY`; la prova parte anche su un webhook disattivato (azione esplicita dell'ADMIN, serve a verificare l'endpoint prima di attivarlo); nessun tipo di fatto o nome assente ⇒ `422`. Opzione conservativa, salvo la prova su webhook disattivato (alternativa: `409`). | APERTA |
| Q-186 | web `lib/persona/cookie.ts`, layout del backoffice, proxy `app/api/lh` · TB-WEB-PERS-015 | docs/07 §4 elenca 5 ruoli ma non dice cosa fare di un cookie `lh_persona` con un ruolo fuori elenco (es. `ROOT`). | Oggi il cookie è accettato: `can()` nega tutto, il layout ricava il ruolo dallo username ma il proxy inoltra in `X-LH-Actor` il ruolo del cookie. **Non è la scelta conservativa** (UI e intestazione verso i servizi non coincidono). Proposta, non implementata: ruolo fuori elenco trattato come `ANALYST` (sola lettura) sia nell'interfaccia sia in `X-LH-Actor`. | APERTA |
| Q-187 | web `lib/persona/cookie.ts` · TB-WEB-PERS-028 | docs/07 §4 non dice che ruolo dare a uno username diverso dalle 5 personas. | `ANALYST` (sola lettura): conservativo. | APERTA |
| Q-188 | web `app/api/persona/route.ts` · TB-WEB-PERS-033…035 | Nessuna fonte fissa la risposta a un corpo di persona non valido (kind assente, MEMBER senza `memberId`, corpo non JSON). | `400 INVALID_PERSONA`, nessun cookie scritto (la persona corrente resta). | APERTA |
| Q-189 | web `app/api/lh/[service]/[...path]/route.ts` · TB-WEB-PRX-015 | docs/07 §3 non dice cosa risponde il proxy per un servizio fuori dagli 8. | `404 UNKNOWN_SERVICE`, nessuna chiamata a valle. | APERTA |
| Q-190 | web `lib/api/client.ts` · TB-WEB-CLI-005 | docs/06 §2 prevede il problema RFC 9457 con `code`; nulla dice come nominare un errore senza `code` né `type`. | Codice `HTTP_<status>`. | APERTA |
| Q-191 | web `lib/nav.ts` `activeHref` · TB-WEB-NAV-041, 042 | docs/08 §1 dice «voce attiva con barra teal» senza il caso di un percorso che non ha voce (`/backoffice/unknown`, `/backoffice/membersX`). | Si accende *Dashboard* (voce di `/backoffice`). | APERTA |
| Q-192 | web `components/bo/LifecycleBar.tsx`, `lib/approvals/queue.ts` · TB-WEB-LIFE-041…045, TB-WEB-APR-018 | docs/08 §3.3 offre da DRAFT *Invia in revisione* **oppure** *Pubblica* secondo la policy, ma non dice cosa mostrare se la policy non è nota (servizio che dorme o in caricamento). | Entrambe le vie: una pubblicazione non ammessa è respinta dal servizio con `409 APPROVAL_REQUIRED` e la barra invita a *Invia in revisione* (LIFE-052). Il vincolo resta lato servizio, quindi nessun oggetto sfugge alla revisione; alternativa più prudente per l'interfaccia, non implementata: mostrare solo *Invia in revisione* finché la policy non è nota. | APERTA |
| Q-193 | web `lib/approvals/queue.ts` `toApproveBy` · TB-WEB-APR-025 | docs/06 §7 dà `requiredRole` in ogni voce della coda, ma non dice cosa vale se manca. | Vale `LEGAL` (unico ruolo approvatore della policy): conservativo. La barra del ciclo di vita usa lo stesso default per "In attesa di LEGAL". | APERTA |
| Q-194 | web `lib/approvals/queue.ts` · TB-WEB-APR-030, 032 | docs/08 §BO-21 non fissa l'ordine delle schede *Da approvare* e *Inviate da me*. | *Da approvare*: chi aspetta da più tempo prima, a parità di invio per codice; *Inviate da me*: dal più recente. | APERTA |
| Q-195 | web `lib/approvals/queue.ts` `outcomeOf`, `formatActor` · TB-WEB-APR-033…040 | docs/08 §BO-21 e §3.6 (`ActorStamp`) non danno le parole dell'esito né il formato dell'attore. | Esiti «In attesa», «Rifiutato», «Approvato», «Approvato e pubblicato» (senza decisione: il codice di stato); attore «username (RUOLO)», «—» se assente, il valore così com'è se senza «:». | APERTA |
| Q-196 | web `lib/campaign/describe.ts` · TB-WEB-DESC-005, 006, 030, 038…043, 051 | L'esempio di BO-06 non copre: tipo azione senza nome, bozza senza trigger o senza effetti, segmenti, coupon/badge/messaggio, più limiti per membro. | Codice del tipo; "un'azione"; codici dei segmenti; "un coupon", "un badge", "il messaggio MSG-…" / "un messaggio"; codice di un effetto sconosciuto; "nessun effetto"; solo il primo limite. | APERTA |
| Q-197 | web `lib/campaign/conditions.ts` · TB-WEB-COND-042, 046 | docs/08 §BO-06 chiede l'intersezione dei campi `data.*` tra più trigger e un avviso sui campi non comuni, ma non il caso di un trigger il cui catalogo non è ancora arrivato (ingestion che dorme). | Oggi quel trigger è ignorato nell'intersezione e negli avvisi: **non è la scelta conservativa** (si possono offrire campi che il trigger mancante non ha, senza avviso). Proposta, non implementata: con un catalogo parziale segnalare "campi non verificati per …" e non dichiarare comune nessun campo finché tutti i cataloghi non sono arrivati. | APERTA |
| Q-198 | web `lib/campaign/conditions.ts` · TB-WEB-COND-087…089 | docs/03 §3.3 e BO-06 non dicono quali percorsi scritti a mano accettare nel costruttore. | Ammesso solo se inizia con `data.`, `member.`, `context.` o `history.` e ha un nome dopo il punto; la validazione del servizio (`POST /v1/campaigns/validate`) resta l'ultima parola. | APERTA |
| Q-199 | web `lib/format/points.ts` · TB-WEB-FMT-011, 012 | docs/07 §9 dà i punti come interi ("1.850"); nulla dice come mostrare un valore decimale. | Arrotondamento it-IT a 0 decimali (metà per eccesso). | APERTA |
| Q-200 | web `lib/format/dates.ts` · TB-WEB-FMT-027, 031, 032, 033, 036 | docs/07 §9 dà solo "3 min fa": non la data assente, la resa sotto il minuto, la forma delle ore né gli istanti futuri. | "—" per una data assente; "ora" sotto il minuto e per il futuro; "N h fa" con le ore per difetto (23 h 40 min → "23 h fa", FMT-034). | APERTA |
| Q-201 | web `lib/reward/portal.ts` · TB-WEB-RWD-054, 056, 057 | docs/09 §PT-03 e docs/03 §5 non coprono una fascia a 0 punti, un premio riservato a più livelli, `lockedByTier` vuoto. | Fascia a 0 raggiunta (barra piena); "Riservato a GOLD e PLATINUM"; elenco vuoto = non riservato. | APERTA |
| Q-202 | web `lib/reward/portal.ts` · TB-WEB-RWD-064…066, 070, 075 | docs/09 §PT-13 non elenca le parole di alcuni stati, non dice quando smettere di attendere un coupon che non arriva né l'ordine dei coupon attivi. | "Coupon emesso", "Completata", "Annullata da te"; con `needsAttention` (pool vuoto) l'attesa si chiude e resta lo stato; coupon attivi dalla scadenza più vicina. | APERTA |
| Q-203 | web `app/portal/page.tsx` · TB-WEB-HOME-012, 016 | docs/09 §PT-01 cita la data nell'avviso di scadenza senza dire cosa fare se manca; §2 esclude `ANONYMIZED` dalle persone ma non dice cosa mostrare se ci si arriva comunque (cookie precedente). | Nessun avviso di scadenza senza data (conservativo: nessuna data inventata); per `ANONYMIZED` avviso di profilo anonimizzato e saluto senza nome. | APERTA |
| Q-204 | web `components/shared/QueryState.tsx` · TB-WEB-QST-006 | docs/07 §6 rende obbligatorio lo stato *Empty*, ma è la vista a sapere cosa è "vuoto". | Senza regola di vuoto (`isEmpty`) si mostra il contenuto, anche vuoto. | APERTA |
| Q-205 | web `lib/demo/reset.ts` · TB-WEB-CONF-008, 013 | docs/08 §3.5 chiede di digitare il codice o l'ID per le azioni irreversibili senza dire se gli spazi ai bordi contano. | Spazi ai bordi tollerati (il testo digitato deve comunque coincidere). Scelta non la più severa ma innocua: l'intenzione dell'operatore resta esplicita. | APERTA |
| Q-206 | web `lib/member/status.ts` · TB-WEB-MBR-005, 008, 009 | docs/08 §BO-03 non dice quali voci di stato offrire per un membro con stato non noto, né le parole degli errori. | Oggi stato non noto = `ACTIVE` (compaiono *Blocca* e *Disattiva*): **non è la scelta conservativa**. Proposta, non implementata: voci disabilitate finché lo stato non è noto. Errori: servizio che dorme → invito a riprovare a demo accesa; codice non previsto → `detail` del problema. | APERTA |
| Q-207 | web `lib/api/status.ts` · TB-WEB-HUB-037…039 | docs/07 §8 (HUB-01) non fissa il formato del tempo trascorso durante il risveglio. | "m:ss"; valore negativo → "0:00". | APERTA |
| Q-208 | web `lib/campaign/describe.ts`, `components/bo/GeneratedSentence.tsx` · TB-WEB-DESC-001, 007 | docs/08 §BO-06 prevede nella sezione *Quando* le «fonti ammesse (default tutte)» e le cita nella frase ("… da ecommerce o app"), ma campaign-service non modella le fonti ammesse della campagna (né tabella né API, `docs/servizi/campaign-service.md`). | La frase elenca le fonti quando la bozza le porta (`CampaignDraft.sources`); le viste oggi non le passano, perché il servizio non le espone (default "tutte" = nessuna menzione). Serve decidere se aggiungere `allowedSources` a campaign (campo opzionale dell'API, nessun contratto evento cambiato) o togliere le fonti dall'esempio di BO-06. | APERTA |
| Q-209 | campaign · `code` in DRAFT (TB-CMP-EDT-031) | BO-06 sez. 1 dice «codice modificabile solo in DRAFT»; `Q-51` (scelta registrata) lo vuole sempre immutabile; la scheda servizio tace. | Vale `Q-51` (fonte registrata): `409 CODE_IMMUTABLE` in ogni stato, anche in DRAFT (il `code` è già nei registri, negli `effectId` e nei riferimenti del portale). Da allineare il testo di BO-06 sez. 1. | APERTA |
| Q-210 | campaign · pubblico `all=true` con liste (TB-CMP-AUD-002…009) | docs/03 §3.2 non dice se `all=true` prevale su `tiers`/`segments` non vuoti. | Attuale: prevale (`all=true` ⇒ tutti). **Non è l'opzione conservativa**: proposta, senza implementarla, che liste non vuote restringano sempre (l'editor le mostra come filtri). | APERTA |
| Q-211 | campaign · pubblico senza restrizioni e `all` non vero (TB-CMP-AUD-010, AUD-019) | `{all:false, tiers:[], segments:[]}` (o `all` assente): nessuno o tutti? | Attuale: tutti. **Non è l'opzione conservativa**: proposta «nessuno» (`AUDIENCE`); la creazione mette già `{all:true}` quando il pubblico manca. | APERTA |
| Q-212 | campaign · `tiers` e `segments` insieme (TB-CMP-AUD-015, AUD-017, AUD-024, AUD-026) | Basta uno dei due (OR) o servono entrambi (AND)? Il pubblico dei contenuti usa AND. | Attuale: OR. **Non è l'opzione conservativa** (AND restringe): proposta AND, da decidere insieme al pubblico dei contenuti. | APERTA |
| Q-213 | campaign · pubblico assente (TB-CMP-AUD-028) | `audience` `null` nel motore. | Attuale: come «tutti» (via API non accade: la creazione mette `{all:true}`). Opzione conservativa: nessuno; impatto solo su dati scritti fuori dall'API. | APERTA |
| Q-214 | campaign · chiavi di pubblico non previste (TB-CMP-AUD-031, AUD-032) | `labels`, `attributes` nel pubblico non sono in docs/03 §3.2 né in F-CMP-06. | Attuale: ignorate (pubblico non ristretto). **Non è l'opzione conservativa**: proposta di rifiutarle alla validazione (`422`). | APERTA |
| Q-215 | campaign · testo numerico contro numero con `eq`/`neq`/`in`/`nin` (TB-CMP-OPS-011) | «Tipi incompatibili → falsa» (docs/03 §3.3): `"5"` e `5` sono incompatibili o lo stesso valore? | Attuale (dopo la rimozione del ripiego testuale): il testo numerico si confronta come numero, come per `gt/gte/lt/lte` (`// SPEC-GAP: Q-215` in `ConditionEvaluator`). **Non è l'opzione conservativa** (sarebbe falsa): da decidere con Q-216. | APERTA |
| Q-216 | campaign · testo numerico con comparatore numerico (TB-CMP-OPS-032) | `"60" gt 50`. | Attuale: convertito in numero, vera. **Non è l'opzione conservativa**: proposta falsa (tipo incompatibile), insieme a Q-215. | APERTA |
| Q-217 | campaign · `between` con estremi invertiti (TB-CMP-OPS-072) | `[20,10]` non è trattato. | Attuale: falsa (conservativa). Proposta aggiuntiva: rifiutarlo alla validazione. | APERTA |
| Q-218 | campaign · comparatore fuori elenco (TB-CMP-OPS-078) | Nessuna regola. | Attuale: foglia falsa (conservativa). Proposta aggiuntiva: rifiuto alla validazione (Q-242). | APERTA |
| Q-219 | campaign · `cmp` assente (TB-CMP-OPS-079) | Nessuna regola. | Attuale: `eq` di default. **Non è l'opzione conservativa**: proposta foglia falsa e rifiuto alla validazione. | APERTA |
| Q-220 | campaign · `member.age` per i nati il 29 febbraio (TB-CMP-MEM-018) | Negli anni non bisestili il compleanno cade il 28/2 o il 1/3? | Attuale: 1° marzo (gli anni si compiono più tardi: opzione conservativa). | APERTA |
| Q-221 | campaign · `member.registeredDaysAgo` (TB-CMP-MEM-023) | Giorni da 24 h o giorni di calendario di Roma? | Attuale: giorni da 24 h, come `history.daysSinceLastAction`. Neutra (conservativa per `gte`, non per `lt`): da decidere. | APERTA |
| Q-222 | campaign · gruppo `any` senza regole (TB-CMP-GRP-007) | Nessuna regola. | Attuale: vero. **Non è l'opzione conservativa** (falso, come l'«esiste» su insieme vuoto): proposta falsa o rifiuto alla validazione. | APERTA |
| Q-223 | campaign · operatore di gruppo sconosciuto (TB-CMP-GRP-019) | `op` fuori da `all/any/not`. | Attuale: trattato come `all`. **Non è l'opzione conservativa**: proposta gruppo falso e rifiuto alla validazione. | APERTA |
| Q-224 | campaign · foglia senza `field` (TB-CMP-GRP-020) | Nessuna regola. | Attuale: vera. **Non è l'opzione conservativa**: proposta falsa e rifiuto alla validazione. | APERTA |
| Q-225 | campaign · condizioni `null` (TB-CMP-GRP-021) | Nessuna regola. | Attuale: nessuna condizione, vera (come `{op:all, rules:[]}` messo dalla creazione). Coerente con «campagna senza condizioni». | APERTA |
| Q-226 | campaign · `GRANT_POINTS` senza `mode` (TB-CMP-PTS-011) | Nessuna regola. | Attuale: `FIXED`. Proposta conservativa: `mode` obbligatorio alla validazione. | APERTA |
| Q-227 | campaign · `ROUND` sulla metà esatta (TB-CMP-PTS-019) | Per eccesso o al pari? | Attuale: per eccesso (130,5 → 131). **Non è l'opzione conservativa** (al pari o per difetto dà meno punti): da decidere. | APERTA |
| Q-228 | campaign · `unitStep` ≤ 0 (TB-CMP-PTS-033) | Nessuna regola. | Attuale: trattato come 1. **Non è l'opzione conservativa**: proposta di rifiutarlo alla validazione e di scartare l'effetto nel motore. | APERTA |
| Q-229 | campaign · `rounding` sconosciuto (TB-CMP-PTS-034) | Nessuna regola. | Attuale: `FLOOR` (la più conservativa). Proposta aggiuntiva: rifiuto alla validazione. | APERTA |
| Q-230 | campaign · `GRANT_PLAYS` senza `count` (TB-CMP-OEF-002) | docs/03 §3.4 elenca `count` senza default. | Attuale: 1 giocata (il minimo utile). | APERTA |
| Q-231 | campaign · parametro obbligatorio mancante nel motore (TB-CMP-OEF-003, OEF-004, OEF-008, OEF-010) | La validazione lo impedisce, ma il motore può leggere dati scritti prima. | Attuale: come `Q-73`, campagna scartata `EFFECT_NOT_SUPPORTED_YET` (niente effetti a metà). Ora la validazione rifiuta anche `ISSUE_COUPON` e `AWARD_BADGE` senza codice. | APERTA |
| Q-232 | campaign · coupon non risolvibile dall'azione (TB-CMP-OEF-007) | `rewardCodeField` assente dal payload. | Attuale: la campagna scatta senza coupon e consuma i limiti. **Non è l'opzione conservativa** per il membro né coerente con `Q-73`: proposta di scartare la campagna. | APERTA |
| Q-233 | campaign · effetti vuoti o non in elenco nel motore (TB-CMP-OEF-017, OEF-019) | La validazione li rifiuta. | Attuale: vuoti ⇒ la campagna scatta senza effetti (consuma i limiti); non elenco ⇒ `EFFECT_NOT_SUPPORTED_YET`. Proposta conservativa: scartare anche l'elenco vuoto. | APERTA |
| Q-234 | campaign · periodo `EDITION` dei limiti (TB-CMP-LIM-008, LIM-025) | Le edizioni sono di wallet; campaign non può leggerle. | Attuale: anno solare di Roma (come `Q-59` per gamification, non registrato per campaign). Da confermare estendendo `Q-59`. | APERTA |
| Q-235 | campaign · limite senza periodo (TB-CMP-LIM-010) | Nessuna regola. | Attuale: «sempre» (il più restrittivo: conservativo). | APERTA |
| Q-236 | campaign · periodo sconosciuto (TB-CMP-LIM-011) | Nessuna regola. | Attuale: chiave unica «sempre» (conservativo). Proposta aggiuntiva: rifiuto alla validazione. | APERTA |
| Q-237 | campaign · accredito oltre il budget residuo (TB-CMP-LIM-035) | «Budget esaurito» = consumato ≥ max; l'accredito che supera il residuo non è trattato. | Attuale: accredito pieno (docs/17 US-E03-09 lo legge così). **Non è l'opzione conservativa**: proposta di ridurlo al residuo; stessa scelta per `perMemberPoints` (`Q-165`). | APERTA |
| Q-238 | campaign · ora finale della fascia (TB-CMP-SCH-017) | `hours: [9, 18]` include le 18:59:59? | Attuale: sì. **Non è l'opzione conservativa** (fine alle 18:00): da decidere con il testo di BO-06 sez. 7. | APERTA |
| Q-239 | campaign · fascia a cavallo della mezzanotte (TB-CMP-SCH-019) | `hours: [22, 2]`. | Attuale: mai dentro (conservativo ma sorprendente). Proposta: rifiuto alla validazione se inizio > fine. | APERTA |
| Q-240 | campaign · giorno della settimana minuscolo (TB-CMP-SCH-023) | `daysOfWeek: ["sat"]`. | Attuale: accettato. Proposta: normalizzare in maiuscolo al salvataggio. | APERTA |
| Q-241 | campaign · `mode` sconosciuto alla validazione (TB-CMP-VAL-036) | La scheda §5 non lo elenca. | Attuale: valida, poi `EFFECT_NOT_SUPPORTED_YET` nel motore (conservativo a valle). Proposta: rifiuto `422` al salvataggio. | APERTA |
| Q-242 | campaign · comparatore sconosciuto alla validazione (TB-CMP-VAL-037) | §3 dice «valida struttura di condizioni», §5 non lo elenca. | Attuale: valida (foglia falsa nel motore). Proposta: rifiuto `422`. | APERTA |
| Q-243 | campaign · `startAt` non ISO senza `endAt` (TB-CMP-VAL-038) | La validazione controlla le date solo in coppia. | Attuale: valida; il motore poi solleva un'eccezione su ogni azione di quel tipo. **Non è l'opzione conservativa**: proposta di rifiutare ogni data non ISO (`422`). | APERTA |
| Q-244 | campaign · azione di transizione sconosciuta o assente (TB-CMP-LCX-001, LCX-002) | `400` o `422`? | Attuale: `422 INVALID_ACTION` (errore di validazione del corpo). | APERTA |
| Q-245 | campaign · `ACTIVATE` (TB-CMP-LCX-003) | Non è un'azione di docs/03 §3.6. | Attuale: sinonimo di `PUBLISH`. Proposta conservativa: rifiutarla (`422`) dopo aver verificato che nessun client la usi. | APERTA |
| Q-246 | campaign · azione in minuscolo (TB-CMP-LCX-004) | Maiuscole dell'azione. | Attuale: accettata (`publish` = `PUBLISH`); innocua. | APERTA |
| Q-247 | campaign · scritture di LEGAL e CARE (TB-CMP-ROL-003, ROL-004, ROL-010, ROL-011, ROL-017, ROL-018, ROL-024, ROL-025) | docs/08 §2 nega `object.edit` a LEGAL e CARE, ma senza ●; docs/06 §3 obbliga il backend a rifiutare solo ANALYST. | Attuale: `403` per ogni scrittura (crea, modifica, duplica, invia) di LEGAL e CARE, con la guardia ADMIN/MARKETING anche su `POST /v1/campaigns`: l'opzione conservativa. | APERTA |
| Q-248 | campaign · `memberDescription` su `LIVE` (TB-CMP-EDT-006) | È la «descrizione» dei campi sicuri (docs/03 §3.6)? | Attuale: sì, modificabile (solo testo per il portale, nessun effetto sul motore). | APERTA |
| Q-249 | campaign · `PUT` senza `version` (TB-CMP-VER-003) | `Q-112` non dice cosa fare. | Attuale: nessun controllo. **Non è l'opzione conservativa**: proposta di rendere `version` obbligatoria (`409`/`428`); l'editor la invia già. | APERTA |
| Q-250 | campaign · copia di una campagna di sistema (TB-CMP-DUP-006) | La copia resta di sistema? | Attuale: no (la copia è una bozza normale, archiviabile). | APERTA |
| Q-251 | campaign · motivo `NOT_LIVE` (TB-CMP-SIM-006, SIM-007) | F-CMP-09 elenca `NOT_LIVE`; docs/03 §3.5 considera candidate solo le `LIVE` e la scheda servizio rimanda a §3.5 «senza deviazioni». | Attuale: le campagne non `LIVE` non compaiono nei risultati. Proposta: allineare F-CMP-09 (togliere `NOT_LIVE`). | APERTA |
| Q-252 | `campaign-service/CampaignEngine.java` · ambito del `MULTIPLIER` | docs/03 §3.4: `scope: ALL_GRANTS` oppure `labels[]`; non dice cosa fare con uno `scope` diverso e senza `labels`. | Nessuna campagna nell'ambito: il moltiplicatore non moltiplica (`// SPEC-GAP: Q-252`). Con `labels[]` non vuote vale l'intersezione con le etichette della campagna dell'accredito. | APERTA |
| Q-253 | `campaign-service/CampaignAdminService.java` · `code` della copia oltre 40 caratteri | `<code>-COPY-n` può uscire dal formato `^[A-Z][A-Z0-9-]{2,39}$` (docs/06 §2) se l'originale è lungo. | Si accorcia la base, mai il suffisso, primo `n` libero (come `Q-113` per i concorsi; `// SPEC-GAP: Q-253`). | APERTA |
| Q-254 | `campaign-service/CampaignAdminService.java` · `requiresLegal` nel `PUT` | BO-06 sez. 1 lo mette nell'editor; toglierlo da una bozza (es. la copia di `CMP-BLACK-FRIDAY`) aggirerebbe l'approvazione LEGAL. | Impostabile solo alla creazione; il `PUT` lo ignora (`// SPEC-GAP: Q-254`). Proposta: nel `PUT` ammettere solo `false → true`. | APERTA |
| Q-255 | ingestion · risoluzione del subject (TB-ING-MBR-025…033) | F-ING-03 e ingestion §5.7 prevedono solo `member:`/`external:`/`email:`; non dicono cosa fare di un subject senza prefisso, con prefisso sconosciuto (`phone:`), `member:` vuoto o `external:` con maiuscole diverse. | In uso: senza prefisso ⇒ tentativo come id membro (`ACCEPTED`/`MEMBER_NOT_ACTIVE` se esiste); prefisso sconosciuto, `member:` vuoto ⇒ `UNMATCHED`; `external:` confronto esatto. **Non è l'opzione più conservativa** per il subject senza prefisso: si propone `UNMATCHED` (parcheggiato, recuperabile con *Abbina*) per ogni forma non prevista. Non implementato. | APERTA |
| Q-256 | ingestion · dedup dopo un esito non accettato (TB-ING-DUP-006…008) | F-ING-02 dice «già visto», ma non se un invio `REJECTED`/`UNMATCHED` conti per la dedup. | Conta solo un `ACCEPTED`: un reinvio corretto con lo stesso id viene rivalutato (può diventare `ACCEPTED`); due rifiuti restano due righe `REJECTED`. È già l'opzione conservativa sui punti (mai due accrediti per lo stesso (fonte, id)). | APERTA |
| Q-257 | ingestion · finestra temporale al cambio dell'ora (TB-ING-TIM-022, TIM-024) | «Più vecchio di 30 giorni» (§5.5): 720 ore o 30 giorni di calendario Europe/Rome? | In uso: 720 ore (`Duration.ofDays(30)`), indipendente dal fuso; nei due giorni del cambio dell'ora la finestra differisce di un'ora da quella di calendario. | APERTA |
| Q-258 | ingestion · `source` in forma breve o URN vuoto (TB-ING-SRC-067, SRC-070) | docs/05 §2 vuole `urn:loyaltyhub:source:<codice>`; non dice se una forma breve (`ecommerce`) sia un errore di forma, né come trattare `urn:loyaltyhub:source:` senza codice. | In uso: forma breve (senza `:`) normalizzata e accettata (la usano simulatore, scenari e transazioni); URN con codice vuoto ⇒ `REJECTED/SOURCE_DISABLED` (nessun 400). URN di servizio o estraneo ⇒ `SOURCE_DISABLED` (confronto esatto, TB-ING-SRC-068/069). **La forma breve non è l'opzione più conservativa** su `POST /v1/events`: si propone di accettarla solo dai chiamanti interni e di rispondere `400` alle fonti esterne. Non implementato. | APERTA |
| Q-259 | ingestion · subject sulla riga `ACCEPTED` (TB-ING-ACC-006) | La scheda non dice se `inbound_event.subject` di un ingresso accettato sia l'originale o il normalizzato; per riprova/abbina Q-118 conserva l'originale. | In uso: la riga `ACCEPTED` porta `member:<id>` (come l'envelope pubblicato); il subject originale resta nel dettaglio solo per le righe risolte a mano. Si propone di conservare sempre l'originale per provenienza. Non implementato. | APERTA |
| Q-260 | ingestion · limiti e default dei tipi azione (TB-ING-ETY-009, 010, 015, 016, 022, 027, 029, 057) | F-ING-06 e Q-89 non fissano lunghezze massime, categoria/abilitazione/`sampleData` di default, né l'effetto di un nome vuoto su un tipo SYSTEM. | In uso: codice e nome ≤ 60 caratteri; categoria assente ⇒ `ENGAGEMENT`; `enabled` assente ⇒ `true`; `sampleData` assente ⇒ `{}` (se valido per lo schema); nome vuoto su un tipo SYSTEM ⇒ nome invariato. | APERTA |
| Q-261 | lh-common · `X-LH-Actor` non valido (TB-ING-ETY-034, RES-007, RES-014) | docs/06 §3 tratta l'intestazione assente come ANALYST, ma non dice nulla di un valore malformato o di un ruolo sconosciuto (`PIRATA:x`). | In uso: trattato come ANALYST (sola lettura ⇒ `403 FORBIDDEN_ROLE` su ogni scrittura). È l'opzione conservativa. | APERTA |
| Q-262 | ingestion · *Abbina* (TB-ING-RES-050…054) | La scheda non fissa i codici d'errore di `POST /v1/inbound-events/{id}/match` né la pulizia di `memberId`. | In uso: `memberId` assente, vuoto o di soli spazi (anche corpo assente o `{}`) ⇒ `422 MEMBER_REQUIRED`; membro non indicizzato ⇒ `422 MEMBER_NOT_FOUND`; spazi attorno all'id ignorati. | APERTA |
| Q-263 | ingestion · `POST /v1/sources` (TB-ING-FON-003) | ingestion §3 elenca `POST /v1/sources` ma non il corpo, il formato del codice né il `kind` delle fonti nuove (registrata anche come Q-164 dall'analisi trasversale). | Implementato con le scelte più conservative: `{code, name, kind?, enabled?, allowedTypes?, description?}`, solo ADMIN (`program.config`), codice minuscolo 2–40 caratteri (cifre e trattini), nome ≤ 80, solo `HTTP` (`INTERNAL` è la sola `internal` del ponte), tipi ammessi esistenti, `enabled` assente ⇒ `true`, `409 SOURCE_EXISTS`, audit `CREATE`. | APERTA |
| Q-264 | ingestion · variazioni del simulatore (TB-ING-SIM-015) | «Piccole variazioni casuali» del `sample_data` (ingestion §3) non è quantificato né dice quali campi. | Solo i numeri di primo livello: decimali ±20 % (2 cifre), interi ±10 % (almeno ±1), nei limiti `minimum`/`maximum` dello schema; stringhe, enum e oggetti annidati (`items[]`) invariati; due invii consecutivi dello stesso tipo diversi. Casualità senza seme fisso (docs/10 §1.3 riguarda il seed, non le prove interattive). | APERTA |
| Q-265 | ingestion · vincoli tra campi di `data` (TB-ING-SCH-083) | `quiz.completed` con `correctAnswers` > `totalQuestions`: JSON Schema non confronta due campi e docs/05 §3 non lo vieta. | In uso: `ACCEPTED`. **Non è l'opzione più conservativa** (un quiz impossibile può dare punti): si propone un controllo tra campi dopo lo schema ⇒ `REJECTED/INVALID_DATA`. Non implementato. | APERTA |
| Q-266 | ingestion · esito di uno scenario (TB-ING-SCN-023, SCN-025, SCN-026) | ingestion §5 e docs/10 §8 non dicono l'esito dell'esecuzione quando un passo non rispetta `expect` o ha un `at` non valido, né la fonte di un passo senza `source`. | In uso: `expect` non rispettato ⇒ passo `ok=false`, esecuzione comunque `DONE`; `at` non valido ⇒ esecuzione `FAILED`; passo senza `source` ⇒ fonte `simulator`. | APERTA |
| Q-267 | ingestion · espressioni `at` (TB-ING-SCT-008, SCT-016, SCT-018) | docs/10 §1 definisce `@lastWeekday` ma non il giorno feriale, né l'ora inesistente/doppia al cambio dell'ora. | Stessa grammatica dei seed (`SeedDates`): feriale = lunedì–venerdì; ora inesistente ⇒ spostata in avanti (02:30 ⇒ 03:30 CEST); ora doppia ⇒ prima occorrenza (CEST). `@lastWeekday` = giorno precedente a oggi anche se l'orario di oggi è passato (TB-ING-SCT-003). | APERTA |
| Q-268 | ingestion · `count` del simulatore fuori 1–20 (TB-ING-SIM-011…013) | BO-28 dice «ripetizioni (1–20)» ma la scheda non dice se un valore fuori intervallo sia un errore. | In uso: limitato all'intervallo (21 ⇒ 20, 0 o negativo ⇒ 1). **Non è l'opzione più conservativa**: si propone `422` per un valore fuori intervallo. Non implementato. | APERTA |
| Q-269 | ingestion · `POST /v1/transactions` (TB-ING-TXN-003, TXN-007, TXN-012) | Q-49 introduce `kind` senza dire delle maiuscole; la scheda non dice se `amount`/`currency` mancanti siano un errore di forma o un `INVALID_DATA`. | In uso: `kind` senza distinzione di maiuscole; `amount`/`currency` mancanti ⇒ `400` (nulla salvato). Si propone di costruire comunque l'azione e lasciare allo schema il `REJECTED/INVALID_DATA`, così il rifiuto è visibile in BO-26 come per `POST /v1/events`. Non implementato. | APERTA |
| Q-270 | ingestion · tipi custom e fonti (TB-ING-TYP-010, TYP-011) | docs/05 §3 dice che i custom arrivano «da qualsiasi fonte»; ingestion §2 dice che l'elenco dei tipi ammessi vuoto = tutti. Un tipo senza schema non è previsto. | In uso: un custom da una fonte con elenco che non lo contiene ⇒ `TYPE_NOT_ALLOWED` (l'elenco della fonte prevale); un tipo senza `data_schema` (solo da DB, l'API lo impone) ⇒ nessuna validazione. È l'opzione conservativa per le fonti. | APERTA |
| Q-271 | seed · storico del monitor ingressi | ingestion §6 chiede 40 righe con tutti gli esiti, ma docs/10 vuole dati coerenti: un `ACCEPTED` senza l'azione corrispondente in `activity-history.json` contraddirebbe statistiche e storie (Anna senza storico, Elisa senza acquisti, Marco senza bolletta digitale). | `seed/inbound-history.json`: 9 `ACCEPTED` (le sole azioni recenti di `activity-history.json`), 7 `DUPLICATE`, 14 `REJECTED`, 10 `UNMATCHED`; nessuna ripubblicazione; ricaricato a ogni reset (righe `hist-*`). Da decidere se aggiungere azioni recenti allo storico attività per avere più `ACCEPTED`. | APERTA |
| Q-272 | ingestion · filtri `from`, `to`, `q` del monitor | ingestion §3 elenca i filtri senza la semantica. | `from`/`to` su `received_at`, ISO-8601, estremi inclusi, malformati ⇒ `400`; `q` = testo senza maiuscole in id evento, soggetto, membro, tipo, fonte, correlazione, dettaglio del rifiuto; stessi filtri anche su `/counts`. | APERTA |
| Q-273 | reward · `POST /v1/portal/redemptions`, precedenza dei 422 | reward §3 e docs/03 §5 elencano i codici d'errore immediati ma non dicono quale vince quando più condizioni sono vere (testbook TB-RWD-ORD-001…015). | Ordine di oggi, da fissare come regola: `MEMBER_NOT_ACTIVE` → `REWARD_NOT_AVAILABLE` → `TIER_NOT_ELIGIBLE` → `SHIPPING_REQUIRED` → `MEMBER_LIMIT_REACHED` → `REWARD_SOLD_OUT` (prima chi è il membro, poi se il premio esiste per lui, poi le condizioni che cambiano nel tempo). Nessun impatto sui dati: nessun evento in ogni caso. | APERTA |
| Q-274 | reward · correlationId della saga | reward §5: «il `correlationId` della richiesta HTTP diventa quello di tutta la saga». Il proxy aggiunge `X-Correlation-Id` a ogni chiamata (docs/07 §3), ma reward lo ignora e apre un tracciato nuovo con l'id del fatto `requested` (TB-RWD-FUL-028). | Oggi: tracciato nuovo, correlationId restituito nel 202. Proposta: se l'intestazione `X-Correlation-Id` è presente e valida, usarla come correlationId della saga (così BO-25 lega il clic del portale all'intera saga); altrimenti come oggi. Se la frase intende solo il correlationId restituito dal 202, la regola è già rispettata. | APERTA |
| Q-275 | reward · estremi della validità del premio | docs/03 §5 dice «dentro validità» senza estremi (TB-RWD-VIS-043, -046). | Intervallo semiaperto `[validFrom, validTo)`: visibile all'istante di inizio, non più all'istante di fine (come `[startAt, endAt)` degli istanti vincenti, docs/03 §6). Da confermare. | APERTA |
| Q-276 | reward · istante di scadenza del coupon | La specifica non dice se il coupon è ancora usabile all'istante `expiresAt` (TB-RWD-CPN-023, -041). | Oggi scaduto a `expiresAt` (uso 410, job con `expires_at ≤ asOf`), come i lotti del wallet (docs/03 §4.2). **Opzione conservativa verso il membro**: valido fino a `expiresAt` incluso (scaduto solo da `expiresAt` + 1 µs). La differenza è di un istante: si propone di mantenere la coerenza col wallet e scriverlo in reward §5. | APERTA |
| Q-277 | reward · «scadenza = oggi + validity_days» | reward §5 conta in giorni da «oggi», ma il codice somma N × 24 h all'istante di emissione (TB-RWD-FUL-002): l'ora locale si sposta col cambio dell'ora e il coupon scade a metà giornata. | Oggi: emissione + N × 24 h. **Non è l'opzione conservativa verso il membro**: si propone scadenza = fine del giorno (23:59:59.999999, Europe/Rome) di data di emissione a Roma + `validity_days`, come le scadenze di fine mese del wallet. Non implementata. | APERTA |
| Q-278 | reward · annullo (`void`) di codici non emessi o scaduti | docs/03 §5 indica VOID tra gli esiti del ciclo del coupon ma non da quali stati; il codice annulla anche AVAILABLE ed EXPIRED (TB-RWD-CPN-002, -008, -011). | Oggi: VOID da AVAILABLE, ISSUED ed EXPIRED; 409 da USED e VOID. **Opzione conservativa**: VOID solo da AVAILABLE (ritiro di un codice mai emesso) e ISSUED; da EXPIRED 409, così lo storico di un codice scaduto non cambia. Non implementata. | APERTA |
| Q-279 | reward · conteggio del limite per membro | F-RWD-03 non dice quali richieste contano per `per_member_limit` (TB-RWD-VIS-058). | Contano PENDING, CONFIRMED e FULFILLED; REJECTED e CANCELLED no (ripristinano lo stock, docs/03 §5): è anche l'opzione più favorevole al membro. Da confermare. | APERTA |
| Q-280 | reward · stock in casi limite (M7.6) | Non è scritto come comporre lo stock residuo quando: il `PUT` arriva senza `version` (TB-RWD-EDT-049); un premio illimitato diventa limitato con richieste in corso (EDT-051); il totale scende sotto le prenotazioni e poi una richiesta è respinta (EDT-053); il residuo è incoerente (< 0, STK-012). | Oggi: senza `version` vince l'ultima scrittura; da illimitato a limitato il residuo è il nuovo totale (le richieste in corso non si sottraggono); il ripristino fa `least(residuo + 1, totale)`; residuo < 0 = esaurito. **Opzione conservativa (niente vendite oltre lo stock)**: `version` obbligatoria nel `PUT` (422 se assente); residuo sempre = max(0, totale − richieste PENDING/CONFIRMED/FULFILLED non chiuse), anche passando da illimitato e dopo un ripristino. Non implementata. | APERTA |
| Q-281 | reward · regole di catalogo non scritte | Scelte del codice senza fonte: tipo normalizzato in maiuscolo (TB-RWD-CAT-007); `AUTO_COUPON` ammesso senza pool (CAT-010), poi la richiesta resta `needsAttention` (FUL-006); categoria facoltativa (CAT-016); `perMemberLimit` ≥ 1 (CAT-021); codice non modificabile, 409 `CODE_IMMUTABLE` (EDT-029); seconda copia `-COPY2`, `-COPY3`… (EDT-061); soglia di fascia ≥ 1 (BND-012). | Tutte come oggi, tranne una. **Opzione conservativa** per `AUTO_COUPON` senza pool: 422 alla creazione o almeno alla pubblicazione, perché un premio pubblicato così lascia ogni richiesta senza codice. Non implementata. | APERTA |
| Q-282 | lh-common · transizioni dei premi | docs/06 §7 dice solo «DRAFT → LIVE diretto» con approvazione spenta; il codice fa pubblicare anche a SUBMIT (TB-RWD-LCY-095); accetta l'azione in minuscolo (LCY-098); un'azione sconosciuta è 422 `INVALID_ACTION` e non 400 (LCY-099). | Come oggi (comune a campagne, premi, concorsi, vedi TB-GOV). | APERTA |
| Q-283 | reward · identità nel portale e ruoli di retry | L'annullo dal portale senza `memberId` è ammesso su qualunque richiesta (TB-RWD-FUL-025); `retry-fulfilment` non è nella matrice di docs/08 §2 (ROL-015, -018). | Retry ai ruoli di `redemption.handle` (ADMIN, CARE), come oggi. **Opzione conservativa** per l'annullo: `memberId` obbligatorio (400 se assente), coerente con l'identità esplicita del portale (CLAUDE.md §1.6) e con `GET /v1/portal/redemptions`. Non implementata. | APERTA |
| Q-284 | reward · pool, codici e date dei job | Regole del codice senza fonte: prefisso 2–10 `A-Z0-9` (TB-RWD-CPN-061…066), validità 1…3650 giorni con predefinito 90 (CPN-067, -069…071), codici fuori da 1…5000 con 422 `COUPON_COUNT_INVALID` (CPN-083, -084); import: normalizzazione e formato minimo dei codici, 422 `COUPON_IMPORT_EMPTY`, tetto di 5000 (CPN-095…098); codice alla cassa normalizzato (CPN-025); `asOf` come data pura = fine del giorno a Roma (CPN-044, -046, -048; il fine giornata è a 23:59:59.999999 dal fix di CPN-045/047). | Come oggi; da riportare in reward §3 (BO-12 e BO-30). | APERTA |
| Q-285 | reward · effetto `coupon.issue` malformato | reward §5 tratta solo il pool vuoto; il codice manda in DLQ, senza ritentativi, anche premio sconosciuto (`REWARD_NOT_FOUND`), premio senza pool (`COUPON_POOL_MISSING`) ed effetto senza membro (`INVALID_EFFECT`); con `effectId` assente l'idempotenza usa l'id dell'evento (TB-RWD-EFF-005…008). | Come oggi: errori di configurazione non si risolvono ritentando. | APERTA |
| Q-286 | docs/03 §3.6 ↔ reward §3 · campi modificabili in LIVE | docs/03 §3.6 ammette su un oggetto LIVE nome, descrizione, fine, priorità e immagine; reward §3, per i premi, solo `stock_total`, `valid_to` e `image_url`. | Vale reward §3 (fonte n. 3 contro n. 4 di CLAUDE.md §6): nome e descrizione di un premio LIVE sono bloccati (409 `REWARD_LIVE_LOCKED`, TB-RWD-EDT-007, -015). Da allineare in docs/03 §3.6 («salvo restrizioni della scheda del servizio»). | APERTA |
| Q-287 | reward §3 · stato `REJECTED` del premio | reward §3 ammette la modifica in `REJECTED`, ma la macchina comune (docs/03 §3.6) non ha più quello stato: REJECT riporta in DRAFT (migrazione V3). | Premio respinto = DRAFT modificabile (TB-RWD-LCY-003, -123). Da correggere il testo di reward §3 («DRAFT/PAUSED»). | APERTA |
| Q-288 | docs/06 §7 · `GET /v1/approvals?status=IN_REVIEW` | Il parametro `status` è documentato, ma l'endpoint comune lo ignora e restituisce sempre la coda IN_REVIEW (o gli invii di `submittedBy`). | Come oggi (TB-RWD-LCY-122 usa il parametro con esito conforme). Da decidere se togliere il parametro dalla specifica o implementarlo in lh-common. | APERTA |
| Q-289 | gamification · ordine dei controlli della giocata | gamification §3 elenca gli errori della giocata (`CONTEST_NOT_LIVE`, `NO_PLAYS_AVAILABLE`, `DAILY_LIMIT_REACHED`, `MEMBER_NOT_ACTIVE`) ma non l'ordine in cui si controllano: con due condizioni false quale codice esce? (TB-GAM-PLY-053…055, CRD-004, CRD-028) | Come oggi: membro (`MEMBER_NOT_ACTIVE`) → stato e periodo del concorso (`CONTEST_NOT_LIVE`) → tetto giornaliero (`DAILY_LIMIT_REACHED`) → giocate disponibili (`NO_PLAYS_AVAILABLE`). Ogni ramo respinge senza registrare giocate, quindi è già l'opzione conservativa; resta da scrivere l'ordine in gamification §3. | APERTA |
| Q-290 | gamification · identità nella giocata del portale | gamification §3 non dice che codice dà una giocata senza `memberId` o con `memberId` vuoto; il path è `/v1/portal/contests/{code}/play`, mentre docs/06 §2 ammette «id o code nei path». (TB-GAM-PLY-056, PLY-057, PLY-059) | Come oggi: `memberId` assente o di soli spazi → 422 `MEMBER_REQUIRED`; l'id del concorso al posto del codice → 404. Già conservativo (nessuna giocata registrata); da riportare in gamification §3. | APERTA |
| Q-291 | gamification · elenco concorsi del portale | gamification §3 dice «concorsi LIVE» senza citare il periodo; nessuna fonte dice cosa vede un membro non `ACTIVE`. (TB-GAM-PTL-002, PTL-003, PTL-006) | Come oggi: un concorso `LIVE` fuori periodo (non ancora iniziato o con `endAt` passato prima del job di chiusura) non è in elenco; un membro non attivo vede i concorsi con 0 giocate e gratuita non disponibile (la giocata è comunque respinta da `MEMBER_NOT_ACTIVE`). Già conservativo. | APERTA |
| Q-292 | gamification · istante piantato (BO-14) | gamification §3 descrive `plant` solo nel caso riuscito (Q-62 fissa l'istante). Non dice cosa succede con concorso non `LIVE`, premio assente o non del concorso, nessun istante `OPEN` del premio. (TB-GAM-PLT-006…009, PLT-017) | Come oggi: concorso `DRAFT`/`PAUSED` → 409 `CONTEST_NOT_LIVE`; premio assente o sconosciuto → 422 `PRIZE_NOT_FOUND`; nessun istante aperto → 422 `NO_OPEN_INSTANT`. Già conservativo (non si creano istanti né si spostano premi). | APERTA |
| Q-293 | gamification · modifica del concorso fuori da LIVE | docs/03 §3.6 fissa i campi sicuri solo per `LIVE`; Q-51 decide `PAUSED`, `ENDED`, `ARCHIVED` e codice immutabile solo per le campagne. Non fissati nemmeno il codice minuscolo e la distribuzione di default. (TB-GAM-EDT-008, EDT-018, EDT-040, EDT-044, EDT-045, EDT-062, EDT-063) | Come oggi, in analogia con Q-51: `PAUSED` bloccato come `LIVE` (solo nome, descrizione, `endAt`; il resto 409 `CONTEST_LIVE_LOCKED`); `ENDED`/`ARCHIVED` → 409 `CONTEST_NOT_EDITABLE`; codice → 409 `CODE_IMMUTABLE`; distribuzione assente → `UNIFORM`. Il codice minuscolo è normalizzato in maiuscolo (`iw-x` → `IW-X`): innocuo, ma l'opzione più stretta per docs/06 §2 (`^[A-Z][A-Z0-9-]{2,39}$`) sarebbe 422 `CONTEST_INVALID`. Non implementata. | APERTA |
| Q-294 | gamification · generazione degli istanti e `asOf` | gamification §3 non tratta la generazione senza premi né un periodo senza ore utili con `BUSINESS_HOURS` (08–22 di Roma); il formato di `asOf` del job di fine concorso non è fissato (Q-156 lo fissa solo per il wallet). (TB-GAM-INS-017, GEN-033, END-008) | Come oggi: senza premi → 422 `CONTEST_INVALID`; `asOf` data pura = ultimo istante di quel giorno a Roma, come Q-156. **Non conservativo oggi**: `BUSINESS_HOURS` senza ore utili fa uscire `IllegalStateException` dal generatore (500 dall'API). **Opzione conservativa proposta**: 422 `CONTEST_INVALID` («nessun orario 08–22 nel periodo») alla generazione. Non implementata. | APERTA |
| Q-295 | gamification · regole degli obiettivi non scritte | docs/03 §8 non fissa: decimali, segno e tipo del campo di `SUM` né la forma del campo (`data.amount` o `purchase.completed.data.amount` di docs/10); la serie con un'azione più vecchia arrivata dopo; il progresso di un membro senza snapshot; il comparatore sconosciuto nel filtro. (TB-GAM-MET-002, MET-003, MET-005, MET-007, MET-008, MET-013, PRG-018, PRG-021, STK-019, PRG-013, FLT-037) | Come oggi: `SUM` tronca i decimali (45.90 → 45), ignora importi ≤ 0 e campi testuali, accetta la forma di docs/10; la serie con un'azione fuori ordine riparte da 1 sull'unità più vecchia; un membro senza snapshot conta (lo snapshot può arrivare dopo l'azione). **Non conservativo oggi**: un comparatore sconosciuto tra numeri vale come `eq` (vero su valori uguali; `// SPEC-GAP: Q-295` in `AchievementRules`). **Opzione conservativa proposta**: foglia sempre falsa, come `ConditionEvaluator` di campaign-service, e 422 `ACHIEVEMENT_INVALID` al salvataggio di un filtro con comparatore fuori da docs/03 §3.3. Non implementata. | APERTA |
| Q-296 | gamification · configurazione di obiettivi, badge e classifiche | Scelte del codice senza fonte: prefissi obbligatori `ACH-`/`BDG-`/`LDB-`; `DISTINCT_TYPES` con traguardo oltre i tipi osservati; top N tra 3 e 50; metrica, periodo e codice non modificabili. (TB-GAM-ACF-012, ACF-025, ACF-033, BDG-013, LCF-001, LCF-004, LCF-015, LCF-020, LCF-021, LCF-023) | Come oggi: 422 `ACHIEVEMENT_INVALID`/`BADGE_INVALID`/`LEADERBOARD_INVALID` per prefisso, traguardo irraggiungibile e top N fuori da 3…50; 409 `LEADERBOARD_LOCKED` su metrica o periodo (i punteggi raccolti non si ricalcolano); 409 `CODE_IMMUTABLE`. Già conservativo; da riportare in gamification §2–§3 e in BO-15/BO-16. | APERTA |
| Q-297 | gamification · effetti, consegna e nickname | docs/03 §3.4 richiede `count` in `plays.grant` senza default (Q-230 sceglie 1 solo per il motore campagne); nessuna fonte per il credito verso un concorso non `LIVE`, per il codice DLQ di un effetto senza dati o senza membro, per la nota di consegna vuota e per il nickname senza nome. (TB-GAM-GRT-004, GRT-005, GRT-007…009, BDG-005, BDG-006, PRZ-034, NCK-005) | Come oggi: `count` assente → 1 credito (come Q-230); il credito verso un concorso `DRAFT` resta e vale quando il concorso va `LIVE`; effetto senza dati o senza membro → DLQ `INVALID_EFFECT` senza ritentativi; nota di soli spazi → `null`; nessun nome → nessun nickname (il portale mostra il segnaposto). **Non conservativo oggi**: `count` 0 o negativo → 1 credito. **Opzione conservativa proposta**: `count` < 1 → DLQ `INVALID_EFFECT` (nessun credito). Non implementata. | APERTA |
| Q-298 | lh-common `ActorContext.parse` · `X-LH-Actor` non canonico (TB-GOV-ACT-007…014, MRL-073, MRL-074) | docs/06 §3 fissa solo `RUOLO:username` e l'intestazione assente (= ANALYST); Q-261 sceglie ANALYST per un ruolo sconosciuto, ma nessuna fonte tratta ruolo minuscolo, spazi, ruolo senza `:`, `RUOLO:` o `:username`, più di un `:`. | Come oggi: ruolo minuscolo e spazi ai bordi letti in modo permissivo (`marketing:luca` = MARKETING); `LEGAL` senza `:` = LEGAL con username `anonymous`; ruolo sconosciuto (Q-261) o vuoto = ANALYST; username col resto dopo il primo `:` (`ADMIN:marta:extra` → `marta:extra`). **Non conservativo oggi**: `CARE:` vale ANALYST ma `CARE: ` (username di soli spazi) vale CARE, e una forma non canonica può ottenere un ruolo di scrittura. **Opzione conservativa proposta**: riconoscere solo `RUOLO:username` con ruolo noto in maiuscolo e username non vuoto, tutto il resto ANALYST (estende Q-261). Non implementata. | APERTA |
| Q-299 | lh-common `GovernedTransitions` · formato dell'azione e ordine dei controlli (TB-GOV-PRS-009…016, ROL-056…060, SMF-001, ROL-051, ROL-052, ENT-019, ENT-031, ENT-043) | docs/03 §3.6 e docs/06 §7 non fissano: spazi ai bordi dell'azione; quale errore esce con più condizioni violate insieme (ruolo, stato, commento); `ACTIVATE` fuori da campagne. Minuscolo, `SUBMIT` con policy spenta e 422 per l'azione sconosciuta sono già in Q-246, Q-282, Q-244; `ACTIVATE` per le campagne in Q-245. | Come oggi: azione normalizzata (maiuscolo, `trim`); precedenza ruolo (403 `FORBIDDEN_ROLE`) → stato (409 `INVALID_TRANSITION`) → commento (422): è l'opzione conservativa (chi non ha il ruolo non scopre lo stato dell'oggetto). **Incoerenza da decidere**: `ACTIVATE` vale `PUBLISH` sulle campagne ma è 422 `INVALID_ACTION` su premi e concorsi; l'opzione conservativa resta quella di Q-245 (422 ovunque). Non implementata. | APERTA |
| Q-300 | lh-common · override di ADMIN e commento del rifiuto (TB-GOV-OVR-006, OVR-016, CMT-006) | docs/03 §3.6 chiede di marcare l'override di ADMIN quando approva al posto del ruolo richiesto, ma non dice nulla di un oggetto senza approvatore di policy; «commento obbligatorio» non dice se gli spazi non separabili contano. | Come oggi: ADMIN che approva o rifiuta un oggetto con regola `NONE` non è marcato override (non scavalca nessuno). **Non conservativo oggi**: un commento di soli U+00A0 è accettato (`isBlank` non li considera spazi). **Opzione conservativa proposta**: `strip` anche degli spazi Unicode prima del controllo, 422 come il commento vuoto. Non implementata. | APERTA |
| Q-301 | member `MemberService.changeStatus` · casi limite del cambio di stato (TB-GOV-MST-001, MST-010, MST-019, MST-008, MST-016, MST-024, MST-028…031, MST-040) | F-MBR-04 e docs/03 §2 non trattano il passaggio verso lo stesso stato, lo stato in minuscolo, il membro anonimizzato con destinazione non valida, `CLOSED` come filtro (i contratti lo ammettono, Q-139). | Come oggi: stesso stato → 200 senza fatto né audit (idempotente); `blocked` = `BLOCKED` (innocuo, come Q-246); anonimizzato → 409 `MEMBER_ANONYMIZED` qualunque sia la destinazione (controllo di stato prima della validazione: non rivela altro); `status=CLOSED` nell'elenco → 200 con 0 membri. Già conservativo. | APERTA |
| Q-302 | member · campaign · reward · ingestion · engagement · celle «—» senza ● di docs/08 §2 (TB-GOV-MRL-002, 003, 008, 009, 021, 022, 027, 028, 033, 034, 039, 040, 045, 046; MAT-012, 013, 028, 029, 033, 034, 043, 044, 047…049, 053, 054, 072, 073, 077, 078, 082…084) | docs/08 §2 impone il rifiuto lato backend solo nelle celle ●; Q-176 registra la scelta solo per l'engagement. | Come oggi, Q-176 esteso a tutti i servizi: `403 FORBIDDEN_ROLE` a ogni ruolo fuori matrice anche nelle celle senza ●. Opzione conservativa. | APERTA |
| Q-303 | gamification · `GET /v1/contests/{id}/instants/histogram` per CARE e ANALYST (TB-GOV-MAT-009, MAT-010) | docs/08 §2 dà «solo istogramma» degli istanti vincenti solo a MARKETING; per CARE e ANALYST la cella è «—». | Come oggi: l'istogramma (aggregato, senza orari puntuali) è letto da ogni ruolo; l'elenco puntuale resta negato. **Non conservativo oggi.** **Opzione conservativa proposta**: istogramma solo ad ADMIN, MARKETING e LEGAL, 403 `FORBIDDEN_ROLE` a CARE e ANALYST. Non implementata. | APERTA |
| Q-304 | member `MemberService.anonymize` · conferma (TB-GOV-ANO-002, ANO-010) | F-MBR-05 chiede di digitare l'id del membro per confermare, senza dire se gli spazi ai bordi contano né quale errore prevale su un membro inesistente. | Come oggi: conferma confrontata dopo `trim` (innocuo); membro inesistente → 404 prima del controllo della conferma. L'opzione più stretta sarebbe il confronto esatto (422 `CONFIRM_MISMATCH`); non implementata. | APERTA |
| Q-305 | member `MemberAttributes` · formato di valori e definizioni (TB-GOV-ATV-002, 003, 005, 015, 031, 047, 062, 063, 069; ATD-002…009, 012…016, 028, 029) | F-MBR-03 e member-service §2 danno i tipi ma non: testo vuoto o lungo, `null` in un PATCH, data con ora, chiavi interne; formato della chiave, etichetta, tetto di definizioni. | Come oggi: testo vuoto o di soli spazi e oltre 200 caratteri → 422; `null` rimuove la chiave; `DATE` solo `YYYY-MM-DD`; chiave interna (`story`) rifiutata; chiave camelCase 2–40, etichetta obbligatoria ≤ 60, al massimo 30 definizioni → 422 `ATTRIBUTE_DEFINITION_INVALID`. Già conservativo; da riportare in member-service §2. | APERTA |
| Q-306 | member `AttributeService` · modifica di definizioni in uso (TB-GOV-ATU-006, ATU-009) | Q-93 vieta di togliere o cambiare tipo a una chiave con valori, ma non dice nulla del restringimento delle opzioni né della chiave con spazi ai bordi. | Come oggi: la chiave è normalizzata con `trim` in silenzio; restringere le opzioni lasciando valori fuori elenco è ammesso (200). **Non conservativo oggi**: restano valori non più validi. **Opzione conservativa proposta**, in analogia con Q-93: 409 `ATTRIBUTE_IN_USE` se un valore presente esce dalle opzioni. Non implementata. | APERTA |
| Q-307 | member `SegmentCriteria` · valutazione e forma dei criteri (TB-GOV-CRT-051, 052, 056, 081, 082, 083, 088; CRV-006, 011, 015) | docs/03 §3.3 (per `data.*`) e §10 non fissano per i segmenti: `contains`/`ncontains`/`startsWith` su un attributo DATE; `nin` su una lista; `exists` su una lista vuota; prefisso `member.`; gruppo vuoto; `member.segments` nei criteri di un segmento. Date ISO (Q-91), `not` (Q-90), 29 febbraio (Q-220) e `registeredDaysAgo` (Q-221) valgono anche qui per riferimento (CRT-045…048, 055, 092, 096, 097, 115; CRV-025). | Come oggi: la data vale come testo per i comparatori di testo; `nin` su lista = intersezione vuota (più stretta di «almeno un elemento» di campaign); lista vuota = campo assente; prefisso `member.` facoltativo; gruppo senza regole non valido; `member.segments` non disponibile (niente segmenti di segmenti). Già conservativo. | APERTA |
| Q-308 | member `SegmentService` · API dei segmenti (TB-GOV-SEG-003, 011, 012, 015, 016, 017, 021, 030) | member-service §3 non fissa: anteprima con criteri vuoti (Q-87 dice solo che non includono nessuno); codice minuscolo; nome obbligatorio; statico con membri inesistenti o anonimizzati; codici d'errore di «solo STATIC» e «archiviato»; blocco ottimistico (Q-112 cita solo campagne, premi, concorsi). | Come oggi: anteprima senza criteri → 422 `INVALID_CRITERIA`; codice normalizzato in maiuscolo; nome vuoto → 422 `NAME_REQUIRED`; membro inesistente o anonimizzato → 422 `MEMBER_NOT_FOUND`; 409 `SEGMENT_NOT_STATIC` e 409 `SEGMENT_ARCHIVED`; `version` superata → 409 `VERSION_CONFLICT` (Q-112 esteso ai segmenti). Già conservativo. | APERTA |
| Q-309 | member · codice invito e invitante bloccato (TB-GOV-REF-004, REF-005, REF-022) | Q-61 tratta il codice di un membro non `ACTIVE` alla registrazione, non la normalizzazione del codice né l'invitante bloccato dopo il legame e prima della prima azione qualificante dell'invitato. | Come oggi: codice normalizzato (maiuscolo, `trim`); i due fatti `referral.completed` sono emessi comunque e il motore scarta quello dell'invitante con `NO_MEMBER` (nessun punto a un membro bloccato). Effetto conservativo; l'alternativa (non emettere il fatto dell'invitante) toglie un evento utile ai report. | APERTA |
| Q-310 | lh-common `ApprovalsController` · `requiredRole` di una voce senza obbligo (TB-GOV-APQ-008) | docs/06 §7 dà `requiredRole` in ogni voce della coda; Q-193 sceglie `LEGAL` solo lato web quando manca. | Come oggi: il backend restituisce `null` per una campagna sotto soglia inviata comunque in revisione; il web mostra `LEGAL` (Q-193, conservativo). Da decidere se il backend debba restituire `LEGAL` anche qui, per un solo punto di verità. | APERTA |
