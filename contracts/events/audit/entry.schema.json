{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:loyaltyhub:schema:audit.entry:1",
  "title": "audit.entry — data (docs/05 §6)",
  "description": "Voce di audit; l'attore (lhactor) è obbligatorio a livello di envelope per gli audit.",
  "type": "object",
  "required": ["service", "entityType", "entityId", "action", "summary"],
  "properties": {
    "service": { "type": "string", "minLength": 1, "x-lh-pii": false },
    "entityType": { "type": "string", "minLength": 1, "x-lh-pii": false },
    "entityId": { "type": "string", "minLength": 1, "x-lh-pii": false },
    "action": { "enum": ["CREATE", "UPDATE", "DELETE", "TRANSITION", "ADJUST", "JOB", "RESET"], "x-lh-pii": false },
    "summary": { "type": "string", "x-lh-pii": false },
    "before": { "type": ["object", "null"], "x-lh-pii": false },
    "after": { "type": ["object", "null"], "x-lh-pii": false }
  },
  "additionalProperties": true
}
