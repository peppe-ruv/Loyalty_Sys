{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:loyaltyhub:schema:fact.member.registered:1",
  "title": "fact.member.registered — data (EVT-FACT-01, docs/05 §5)",
  "description": "Snapshot completo del membro (nessun dato personale in chiaro fuori da qui; niente in log/metriche).",
  "type": "object",
  "required": ["memberId", "status"],
  "properties": {
    "memberId": { "type": "string", "pattern": "^MBR-[0-9]{6}$" },
    "firstName": { "type": "string" },
    "lastName": { "type": "string" },
    "nickname": { "type": "string" },
    "email": { "type": "string" },
    "externalId": { "type": "string" },
    "status": { "enum": ["ACTIVE", "SUSPENDED", "BLOCKED", "CLOSED", "ANONYMIZED"] },
    "channel": { "type": "string" },
    "registeredAt": { "type": "string", "format": "date-time" },
    "birthDate": { "type": "string" },
    "city": { "type": "string" },
    "referralCode": { "type": "string" },
    "referredBy": { "type": ["string", "null"] },
    "attributes": { "type": "object" },
    "labels": { "type": "array", "items": { "type": "string" } }
  },
  "additionalProperties": true
}
