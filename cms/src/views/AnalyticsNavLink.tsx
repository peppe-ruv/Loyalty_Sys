"use client";
import React from "react";
/** Voce di menu del pannello verso la vista Andamenti (RF-123). */
export default function AnalyticsNavLink() {
  return <a className="nav__link" href="/admin/analytics" style={{ display: "block", padding: "8px 0" }}>Andamenti (BI)</a>;
}
