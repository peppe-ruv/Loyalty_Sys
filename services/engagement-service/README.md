# engagement-service — porta 8087, schema `engagement` (docs/04 §3, docs/servizi/engagement-service.md): template, regole di notifica e inbox in-app (M6.0); contenuti, pop-up, tema (M6.1–M6.5); webhook in uscita firmati HMAC con ritenti a 1, 5, 15 minuti (M7.2, BO-23): scheduler ogni 30 s attivo anche in demo (`loyaltyhub.webhooks.dispatcher.enabled`), job demo `POST /v1/demo/jobs/deliver-webhooks?asOf=`, ricevitore d'esempio in `deploy/webhook-receiver/`.
