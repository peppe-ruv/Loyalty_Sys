<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.5</version>
    <relativePath/>
  </parent>

  <groupId>io.loyaltyhub</groupId>
  <artifactId>loyalty-hub-services</artifactId>
  <version>${revision}</version>
  <packaging>pom</packaging>
  <name>Loyalty Hub services</name>
  <description>Servizi di dominio del Loyalty Hub vendor neutral</description>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <properties>
    <!-- Target di rilascio: Java 25 LTS (ADR-003). Il valore qui è la baseline minima usata in CI; alzare a 25 quando l'immagine base è disponibile. -->
    <java.version>21</java.version>
    <revision>0.6.2-SNAPSHOT</revision>
    <otel.instrumentation.version>2.18.1-alpha</otel.instrumentation.version>
    <maven.compiler.release>${java.version}</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <cloudevents.version>4.0.1</cloudevents.version>
    <resilience4j.version>2.3.0</resilience4j.version>
    <!-- Il client Docker di Testcontainers 1.21 parla l'API 1.32, che i motori recenti rifiutano.
         1.44 esiste da Docker 25; per un motore più vecchio: -Ddocker.api.version=<versione>. -->
    <docker.api.version>1.44</docker.api.version>
  </properties>

  <modules>
    <module>common</module>
    <module>ingress-adapters</module>
    <module>rules-engine</module>
    <module>ledger</module>
    <module>tier-service</module>
    <module>segment-service</module>
    <module>member-service</module>
    <module>engagement-service</module>
    <module>decision-service</module>
    <module>fraud-service</module>
    <module>catalog-redemption</module>
    <module>contest-service</module>
    <module>identity-mapping</module>
    <module>read-model</module>
    <module>notifier</module>
  </modules>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>io.loyaltyhub</groupId>
        <artifactId>common</artifactId>
        <version>${revision}</version>
      </dependency>
      <dependency>
        <groupId>io.loyaltyhub</groupId>
        <artifactId>common</artifactId>
        <version>${revision}</version>
        <type>test-jar</type>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>io.cloudevents</groupId>
        <artifactId>cloudevents-core</artifactId>
        <version>${cloudevents.version}</version>
      </dependency>
      <dependency>
        <groupId>io.cloudevents</groupId>
        <artifactId>cloudevents-json-jackson</artifactId>
        <version>${cloudevents.version}</version>
      </dependency>
      <dependency>
        <groupId>io.github.resilience4j</groupId>
        <artifactId>resilience4j-spring-boot3</artifactId>
        <version>${resilience4j.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-registry-prometheus</artifactId>
    </dependency>
    <!-- RF-133 API-first: OpenAPI 3.1 generata per ogni servizio su /v3/api-docs e /swagger-ui.html -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>2.8.9</version>
    </dependency>
    <!-- RF-118 tracing distribuito: Micrometer Tracing → OpenTelemetry → OTLP verso il collector -->
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-tracing-bridge-otel</artifactId>
    </dependency>
    <dependency>
      <groupId>io.opentelemetry</groupId>
      <artifactId>opentelemetry-exporter-otlp</artifactId>
    </dependency>
    <!-- RF-118 log strutturati con trace id (ECS) e spedizione OTLP opzionale -->
    <dependency>
      <groupId>io.opentelemetry.instrumentation</groupId>
      <artifactId>opentelemetry-logback-appender-1.0</artifactId>
      <version>${otel.instrumentation.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Test di integrazione su Postgres vero: le migrazioni Flyway, i vincoli e il trigger di
         immutabilità del ledger sono metà della logica e con un database in memoria non si provano. -->
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- I raw type e le conversioni unchecked qui sono costati due errori di compilazione:
           tenerli visibili in ogni build è il modo più economico per non ripeterli. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <compilerArgs>
            <arg>-Xlint:unchecked,rawtypes,deprecation</arg>
          </compilerArgs>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <systemPropertyVariables>
            <api.version>${docker.api.version}</api.version>
          </systemPropertyVariables>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>flatten-maven-plugin</artifactId>
        <version>1.6.0</version>
        <configuration>
          <updatePomFile>true</updatePomFile>
          <flattenMode>resolveCiFriendliesOnly</flattenMode>
        </configuration>
        <executions>
          <execution><id>flatten</id><phase>process-resources</phase><goals><goal>flatten</goal></goals></execution>
          <execution><id>flatten.clean</id><phase>clean</phase><goals><goal>clean</goal></goals></execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
